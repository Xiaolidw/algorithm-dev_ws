# 9.1 回归修复同步记录

更新时间：2026-09-02。Windows 桌面主记录为 `C:\Users\奶龙\Desktop\9.1.md`。

## 已完成

- 磁盘重复工作区已清理，根分区恢复约 7.1 GiB 可用；仅保留两份有效 VM 基线。后续备份统一使用 GitHub 分支 `codex/2026-09-02-regression`。
- 精确对接入口保护由 1.10 m 对齐到既有预接近包络 1.20 m，提交 `d9d93ef`。
- B 区物块弹飞根因是 LinkAttacher 只有一个全局附着槽：放下一块后，下一次放置会解除上一块。已用 Gazebo 物理循环内的多物块锁存集合替代，提交 `ccc67ab`。
- 双物块强制位姿/速度扰动测试通过；完整流程中锁存计数依次达到 1、2、3。
- `blue_cube_1` 在 B 区释放于 `(-2.606938,-5.089399)`，经历后续离场和六轮 Nav2 恢复后仍保持完全相同的 XY、水平且近零速度，B 区弹飞问题已修复。
- 上半区移动障碍物最终冻结为非协作式巡逻：`(-3,3) ↔ (2,3)`、`0.20 m/s`、`stop_distance=0`、`yield_pass_lock_enabled=false`，提交 `cd32239`。日志 `competition_20260902_121808.log` 验证完整往返，无机器人触发让行。

## 最新回归

日志：`/home/ros/dev_ws/logs/competition_20260902_115536.log`。

- 任务 1 完成；`pickup_pre` 一次到达，A 区运输一次恢复。
- 任务 2 完成；抓取中转多次恢复，`DROPOFF_TRANSIT` 在 0.101 m 接受。
- 任务 3 完成；蓝块 1 在 B 区物理放置并锁存。
- 任务 4 在 `PICKUP_PRE` 六次 Nav2 中止后结束，整体 3/5。

任务 4 的路径从约 `(-3.0,1.35)` 指向 `(-5.08,1.70)`，平滑器反复在 `(-4.18,1.26)` / `(-4.01,1.27)` 判碰撞；该位置属于未抓取 `blue_cube_3(-3.95,1.70)` 的真实膨胀区。随后 MPPI 无可行轨迹，倒车和旋转被 Collision Monitor 拒绝。

下一项单变量修复：在 `blue_cube_2` 的 B 区离场点 `(-1.80,-4.40)` 后增加中心通道点 `(-5.08,2.70)`，先从蓝块行上方通过，再沿 x=-5.08 下探；不改障碍半径或全局安全距离。

## 持续约束

- 一次只改一个变量；任务 active 时不改参数、不重启。
- 不恢复高频 ROS 服务式物块跟随。
- 不删除真实地图障碍，不缩小全局安全距离，不放宽物理放置边界。
- 不把局部规划失败归因为“不支持斜线”。
- 移动障碍物 1 的轨迹、速度和非协作规则已经冻结，后续不得修改。

## 2026-09-04：B 区停车与放置前底盘稳定链路

提交：`661a16e feat: stage B zone parking before placement`。

- 旧链路在 B 区直接由 Nav2 到公共终端位 `(-2.75,-5.00,0)`，允许在最高 `0.32 m` 导航残差时将控制权交给机械臂；它没有显式的零速稳定与分段低速进入。
- 新链路仅对 `blue_cube_1/2/3 → B` 生效：Nav2/A* 先到各自终端位东侧约 0.50m 的预接近点；达到 0.16m 即取消无意义的终端航向细调；等待动作句柄退出后连续零速 0.60s；沿安全链路 `/cmd_vel_nav_raw → Collision Monitor` 以最高 0.10m/s 进入；在 0.20m 残差处再零速 0.50s；以最高 0.045m/s 收敛到 0.12m；对齐最终机械臂航向、零速 0.70s 后，才调用既有严格物理放置动作。
- 预接近点放在 B 区东侧，而非向西越过真实木墙 `x=-3.262`：蓝块 1/2 使用 `(-2.25,-5.00,pi)`，蓝块 3 使用 `(-2.10,-4.60,pi)`；末段面向西驶入，最终才原地转回机械臂要求的东向航向。
- 末段在 Collision Monitor 拦截、里程不再改善或超过 35s 时安全失败，并重走完整 Nav2 预接近，不会直接穿过木墙重试；未修改全局安全距离、墙体、物块半径、A 区代码或已冻结的上半区障碍物。

### B-only 预接近验证

日志：`/home/ros/dev_ws/logs/competition_20260904_125249.log`。

- 在任务流 `IDLE`、未抓取/未放置且不进入 A 区的条件下，直接向 `/navigate_to_pose` 发送 B 区预接近目标 `(-2.25,-5.00,pi)`。
- Nav2 动作在约 18s 后 `SUCCEEDED`，`number_of_recoveries=0`；最终 `/odom` 真值为约 `(-2.233,-5.063)`，与预接近目标约 0.065m，落在新 0.16m 交接窗内。
- 已完成：源码 AST 检查、`moon_warehouse_coordinator` 构建、服务/arm/gripper/Nav2 active 检查以及 B-only A* 预接近验证。
- 尚未验收：载蓝块时的本地 0.60s/0.50s/0.70s 停车链路、最终 0.12m 门槛、机械臂释放与多物块稳定性。下次只能从 B 区单段或完整流程的 B 任务中收集该证据；不得把本次无载物的预接近成功表述为完整放置通过。
- B-only 测试后已在任务 IDLE 时重启服务；最新启动日志为 `competition_20260904_125601.log`，服务及任务流均已回到 `active` / `IDLE` 的干净初始状态。

## 2026-09-04：避障实体接触修复与放置进度

- 证据日志：`/home/ros/dev_ws/logs/competition_20260904_160121.log`。`PICKUP_PRE` 时小车真值约为 `(-1.820,2.736)`，移动障碍物 1 为 `(-2.262,3.084)`；进入 `0.60m` 实体碰撞区后，障碍速度由规定 `0.20m/s` 降至约 `0.001m/s` 并离开 `y=3`。预测仍假定巡逻速度，Collision Monitor 仅 `approach` 降速，随后 MPPI 多次无解和 `STALLED_NO_PROGRESS`。
- 已修复：仅对 `moving_obstacle_1` 启用确定性轨道锁，保留实体碰撞体、`(-3,3) ↔ (2,3)` 和 `0.20m/s`，但禁止底盘将其推偏、卡住或让行；Collision Monitor 同一预测风险区改为 `stop`，预测接触即停车，障碍通过后才继续。未缩小全局安全距离或删除任何真实障碍。
- 空闲场景 6 秒真值验证：`x=-2.202 → -0.722`、`y_span=0.0000m`、速度恒为 `0.200m/s`；已构建 `gazebo_physics_obstacle_plugin`、`mybot_description`、`moon_warehouse_bringup`。
- 放置进度：MoveIt FK/Cartesian 已用于上方对位与单次垂直下放；最终路径完成率从 `0.20~0.38` 升至 `0.625→0.750→0.833`，仍未达到严格 `1.000`，因此安全拒绝松爪，无弹飞或虚假放置。下一步须在新避障轨道锁下先复测 `PICKUP_PRE`，再基于真实可达边界仅调整一个 B 工作位或槽位参数。

## 2026-09-04：精确通道安全边界与 B 区放置回归（最新）

- 真实底盘为 `0.32 × 0.32m`，轮组横向外沿为 `±0.210m`。两张代价地图的足迹从 `±0.24m` 收至 `±0.22m`，仍有每侧 `10mm` 物理余量；前向 `0.48m` 的机械臂/载物包络不变。墙体、物块、激光和 Collision Monitor 的硬碰撞约束均未缩小。
- Humble Collision Monitor 在 `stop` 模式若仅配置 `footprint_topic` 会因 `FootprintApproach.points` 缺失而无法启动；已补入同一足迹的扁平点数组。日志 `competition_20260904_162019.log` 后，导航生命周期确认 `Managed nodes are active`，硬制动链恢复。
- 全局 inflation 单独从 `0.70m` 调为 `0.45m`；局部仍为 `0.50m`。在物理横向半宽外仍保留至少 `0.23m` 全局软缓冲，局部足迹碰撞检查和 Collision Monitor 继续提供双重硬否决。
- B 失败证据：旧终端附近底盘约 `(-2.227,-4.989)`，工具前伸约 `0.374m`，垂直笛卡尔段仅 `0.600 → 0.708 → 0.792`。仅将 B 终端基准从 `(-2.20,-5.00,pi)` 向西改为 `(-2.30,-5.00,pi)`；`(-2.0,-5)` 会使前伸再增加约 `0.20m`，不可取。
- 验收日志 `/home/ros/dev_ws/logs/competition_20260904_162633.log`：B-only `blue_cube_1` 在残差 `0.095m` 的分段停车后，上方对位和单次垂直下降均 `100%`；释放真值 `(-2.4999,-5.1180,0.0152)`，平面/高度误差分别 `0.0020/0.0017m`、最大速度 `0.001161m/s`，水平、静止、在 `zone_b` 内且已 secure，任务 `FLOW_COMPLETED`。
- 同轮 B 预接近第一次有一次 `STALLED_NO_PROGRESS`，新鲜预测重试后成功；它与终端/放置无关，需后续独立取证。部署 `0.45m` 全局膨胀后的第三次 B-only 回归日志 `competition_20260904_163151.log` 同样 `FLOW_COMPLETED`。

## 2026-09-04：B 区正向伸臂放置唯一化与复验（最新）

### 旧逻辑清理

- 发现新 YAML 已使用 B 区纵向中心线槽位，但 Python 内置默认仍保留旧的五个侧向槽位；缺失参数文件或换启动入口时会回退到旧斜向放置。现已把内置默认与 YAML 统一为 `zone_b: (-0.09,0.00) → (0.00,0.00) → (0.09,0.00)`，按远到近放置，横向始终为 0。
- 删除运行配置中 `blue_cube_3 → (-2.60,-4.60,0)` 的反向停车覆盖，并把其 B 预接近点统一为 `(-1.55,-5.00,pi)`。三个蓝块现在均使用同一东侧预接近、`(-2.30,-5.00,pi)` 西向停车和正前方伸臂链路，不再存在蓝块 3 的 180° 终端转向/侧向伸臂分支。
- B 最终航向门限独立收紧为 `0.03rad`，避免用肩关节侧摆补偿底盘偏航；几何单次核验容差仅由 `0.035m` 放至 `0.040m`。区域边界、接地、水平、静止、物块间距和 secure 验收均未放宽。
- 活动源码扫描确认旧 B 槽位值和旧蓝块 3 停车点为 0 处；历史备份仍仅作为 GitHub 快照，不参与构建和运行。

### 两次成功证据

1. 修改中心线槽位后的首轮日志：`/home/ros/dev_ws/logs/competition_20260904_165437.log`。
   - 动态放置明确选择 `zone_b slot 1 local=(-0.090,0.000)`，IK residual `0.0015m`。
   - MoveIt 上方对位和单次垂直下降均为 `100%`；释放点约 `(-2.59005,-4.99939,0.01578)`。
   - 单次核验平面误差 `0.0006m`、竖向误差 `0.0008m`；释放后速度 `0`，grounded/stable 均为 True，最终 `FLOW_COMPLETED`。
2. 清除所有旧运行分支并重启后的干净复验日志：`/home/ros/dev_ws/logs/competition_20260904_165926.log`。
   - 运行节点实载槽位为 `[-0.09/0, 0/0, +0.09/0]`；B-only 服务确认未排入 A 区任务。
   - 动态目标为 `cube_target=(0.382,0.021,-0.022)`，横向 2.1cm 仅来自最终停车实测残差，不是侧向槽位；两段 Cartesian 再次均为 `100%`。
   - 释放点约 `(-2.59004,-4.99945,0.01568)`；平面误差 `0.0006m`，释放最大速度 `0.001162m/s`，grounded/stable/secure 全部通过，最终 `FLOW_COMPLETED`。

### 保留的独立待办

- 第二次复验的载物 `B_DROPOFF_PRE` 第一次出现 MPPI optimizer failure、一次 `Collision Ahead` 和 `STALLED_NO_PROGRESS`，既有第 2 次新鲜预测重试后成功抵达。该现象属于载物导航恢复，不是放置代码未调用或放置失败；后续必须单独提取动态障碍、代价地图和控制器证据后再做单一修改。

## 2026-09-04：连续两个蓝块 B 区全链路回归（最新）

### 测试入口与范围

- 新增 `/mission/run_two_b_dropoff_regression`，固定连续队列为 `blue_cube_1 → blue_cube_2`，共 2 项且明确不生成 A 区任务；单块入口与正式完整任务共用同一抓取、导航、B 分段停车、物理放置和 secure 代码。
- 测试日志：`/home/ros/dev_ws/logs/competition_20260904_171154.log`。总耗时约 `271.9s`（4 分 31.9 秒），最终 `FLOW_COMPLETED: Completed 2 pick/place task(s)`。

### 分段结果

1. `blue_cube_1`
   - `pickup_pre`、物理 docking、抓取、载物前往 B 均为第 1 次到达；抓取后相对夹爪抖动为 0。
   - 放入首槽 `(-0.09,0)`；上方对位和垂直下降均 `100%`。单次核验平面/高度误差为 `0.0005/0.0007m`，释放最大速度 `0.001162m/s`，grounded/stable/secure 全通过。
   - 放置真值 `(-2.59004,-4.99947,约0.015m)`。底盘与方块已经物理分离约 `0.38m`，因此没有执行盲目固定距离倒车，而是由下一段 Nav2 经 `(-1.80,-4.40) → (-5.08,2.70)` 地图感知离场。
2. `blue_cube_2`
   - 第一块后的两个离场/中转点、`pickup_pre` 和 docking 全部第 1 次到达。动态抓取 IK 误差 `0.0007m`，夹爪窗口误差 `0.0217m`，抬升后相对抖动为 0。
   - 载物回到 B 的预接近和低速停车一次成功；选择第二槽 `(0,0)`，检测到 `occupied=1`。两段 Cartesian 均 `100%`，单次核验平面/高度误差 `0.0002/0.0008m`。
   - 释放真值 `(-2.49988,-4.99987,约0.015m)`，最大速度 `0.001161m/s`，grounded/stable/secure 全通过。作为最后一项，按既定要求原地停止并结算，不执行无意义退出。

### 结束后物理验收与异常审计

- 结束后再次读取 Gazebo 真值：两块姿态四元数均近似单位姿态，线/角速度近零；第一块在底盘离场、第二块抓取和再次进入 B 后仍保持 `(-2.59003,-4.99947)`，没有被推动或弹飞。
- 两块中心距约 `0.09015m`，扣除两个 3cm 方块后的表面净距约 `0.06015m`，高于 `0.055m` 物理净距门限；均完整位于 B 区。
- 本轮计数：`Optimizer fail=0`、`Collision Ahead=0`、`STALLED_NO_PROGRESS=0`、任务级恢复/重试=0。
- 平滑器有 3 次在 `(-2.85,2.95)`、`(-3.15,3.10)`、`(-4.70,2.85)` 判定平滑结果进入碰撞区；均位于上半区动态障碍轨道附近，约 0.2 秒后行为树把安全的未平滑路径交给控制器，所有对应导航仍 attempt 1 到达。这是安全拒绝和正确回退，不应通过放宽碰撞或强制平滑来“修复”。另有 4 次控制周期未达到 10Hz 和少量 TF 时间外推警告，未造成停滞或动作失败，后续只监测。
- 结论：当前连续两蓝块链路的抓取、载物避障、B 停车、中心线放置、第一块后的地图感知离场、多物块锁存和最后原地结算均通过；本轮不再修改导航或放置参数。

## 2026-09-05：固定障碍物锚定、最近取件与安全巡航提速（最新）

### 障碍物被撞飞的根因与修复

- 截图中的对象不是两条受控移动障碍物，而是世界模型 `Untitled` 内 13 个一米赛场障碍块。其模型原先为 `static=0`、各 link 质量 `1kg` 且没有关节；底盘接触可将它们推离静态地图中的原始位置，导致真实世界和代价地图不一致。
- 旧两蓝块日志 `/home/ros/dev_ws/logs/competition_20260904_171154.log` 的结束审计显示 12/13 个块偏移超过 5mm，最大 `1.2304m`；`link_4` 从约 `(-2.6955,-3.5429)` 被推到 `(-2.4372,-2.3399)` 并倾倒。这正是“障碍被撞飞”的直接证据。
- 已仅将该固定赛场复合模型锚定为 `static=1`，保留全部真实碰撞几何；移动障碍物 1 保持其用户规定的 `(-3,3) ↔ (2,3)`、`0.20m/s` 非避让运动。静态修复后的同一两蓝块流程审计为 13/13 块 `max_delta=0.0`。
- 锚定后暴露出原载物路线曾经依赖把 `link_4` 推开：旧路径在 `x≈-2.28` 向南进入其 `x=[-3.20,-2.20], y=[-4.04,-3.04]` 实体范围。蓝块到 B 区现统一先经固定块东侧的开阔中转点 `(-1.45,-2.40)`，再进入既有 B 预接近点；没有缩小安全距离或删除实体障碍。

### 调度、速度与放置超时清理

- 执行器现在每次启动下一次抓取前，依据实时底盘/未完成物块 Gazebo 位姿，将最近且可开始接近的任务换到当前队首；`red_cube_1` 在上半区轨道门未放行时仍视为暂不可达，不会阻塞其它可安全执行的最近任务。每个物块既有的中转、预接近、精密 docking、Collision Monitor 和 SIPP 门控不变。
- 巡航不再被旧 `0.60m/s` MPPI 上限及 `0.55m/s` 动态策略全局压低：开阔且对齐的 `FAST_CRUISE` 最高 `1.20m/s`、普通巡航 `0.95m/s`、谨慎模式 `0.45m/s`。SIPP 对预测冲突降速，Collision Monitor 仍持有最终停车权；抓取直线进给和 B 区分段停车继续使用独立低速控制。
- 修复一次放置动作结果丢失时的清理顺序：Gazebo 已证实物块接地后，先取消旧动作、发出一次 carry/base-lock 释放、等待 `1.5s` 让服务端收尾，再开始下一项。此前直接推进队列会让 `manipulation base lock` 留在锁定状态，后续底盘只能原地转向并累积 `STALLED_NO_PROGRESS`；该修复不删除或跳过放置动作。

### 最新验证

- 运行日志：`/home/ros/dev_ws/logs/competition_20260905_151718.log`，B-only `blue_cube_1` 生产路径 `FLOW_COMPLETED`。
- 抓取后载物经新的东侧中转、B 预接近和低速停车到位；无 `Optimizer fail`、`Collision Ahead` 或 `STALLED_NO_PROGRESS`。
- 放置完整返回 `place_complete`：物理释放真值约 `(-2.5900,-4.9995,0.0145)`，`grounded=True`、`stable=True`、最大速度 `0`；最终底盘锁明确释放，任务原地结算。
- 后续完整五物块回归应记录最近任务选择顺序、各巡航模式实际速度、每段耗时及恢复次数；运行 active 时仍禁止改参或重启。

## 2026-09-05：地图基准审计、单 RViz 与非最左蓝块回归

- 地图不再改动墙体、石块的坐标、尺寸或碰撞几何。将墙体父坐标临时改为 `0.9981` 后，截图确认会破坏石块与墙的相对布局，已回退该源码修改。
- 以旧地图 `competition_v2_scoring_u22.world` 的 `<state>` 为基准读取 Gazebo 真值：`Untitled` 13 块固定石头逐项最大坐标误差为 `0.000000m`；例如 `link_4=(-2.695500,-3.542920,0.500000)`，相邻 `Wall_75=(-3.262160,-4.855530,0)`。保留 `static=1` 仅防止这些固定赛场障碍物再次被撞飞。
- 一键任务启动原先同时启动导航 RViz 和 MoveIt RViz。现仅保留导航的 `competition_nav_topdown.rviz`；MoveIt 仍后台提供 `move_group`、抓取和放置服务。运行时进程审计确认仅 1 个 `rviz2`。
- 新增逐个回归入口 `/mission/run_next_nonleft_blue_b_dropoff_regression`，顺序为 `blue_cube_2 → 3 → 4 → 5`，每次先重置 Gazebo，避免 B 区槽位占用干扰。
- 地图基准下：`blue_cube_2` 成功抓取、经 B 分段停车并释放于 `(-2.5900,-4.9992,0.0149)`，`grounded/stable=True`、速度和抖动为 0；`blue_cube_4` 同样成功，释放于 `(-2.4998,-4.9999,0.0148)`。
- `blue_cube_3` 抓取成功，但载物 `DROPOFF_TRANSIT → (-1.45,-2.40)` 连续 6 次 `ABORTED`；Collision Monitor 拒绝 Spin/DriveOnHeading 恢复。该问题与地图坐标、抓取、放置无关，后续应只针对该载物局部恢复链取证和最小修复。
- `blue_cube_5` 在任务停止前可到达 pickup 预接近与最终 docking，但后者被 Collision Monitor 恢复拦截并多次超时；该验证为定位记录，未在 active 时更改参数。
