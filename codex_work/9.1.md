# 9.1 工作暂停与交接记录

更新时间：2026-09-01（Asia/Shanghai）  
远程环境：`u22`，用户 `ros`，工作区 `/home/ros/dev_ws`

## 2026-09-01 晚间更新（以本节为准）

### 已确认的暂停状态

- 虚拟机 u22 的 moon-warehouse.service 仍在运行，但 /mission/execution_status 为 IDLE、active=false、task_count=0；当前没有任务在执行。
- 已停止进一步修复、重启和参数变更。本文件仅记录截至暂停时的事实、证据与恢复顺序。
- 虚拟机根分区仅余约 680 MB（约 99% 已用）。恢复验证前应先释放可确认无用的旧日志或扩容，避免日志写满影响仿真与定位；清理前须确认具体目标。

### 已完成并有证据的工作

1. 任务 1 的 PICKUP_PRE 连续 STALLED_NO_PROGRESS 已完成针对性修复与回归。移动障碍物 1 改为基于 model_states 的让行门控：方向锁存、机器人通过 release_x=-1.80 后才允许反向，并保留最小东向速度、状态过期和超时保护；不再依赖 0.5 s 或 8 s 的固定等待。
2. 上半区移动障碍物 1 已按最终约束固定为在 (-3, 3) 与 (2, 3) 之间持续往返，速度 0.20 m/s。后续不应再对它改动。
3. 任务 1 红块已完成定向抓取、斜向搬运及 A 区放置。A 区停车位为 (-7.06, -2.50, -pi/2)，放置槽为 x=-0.12、-0.06、0、0.06、0.12，y=0.12，释放高度 0.005 m。
4. 放置流程已由“时间等待释放”收敛为安全门控：物理插件在松爪后保持 0.35 s；执行器在开爪前锁存 prepare_release；以被夹物块的实际三维位置验证槽位与竖直速度，未满足即拒绝释放并返回 RELEASE_FAILED=8，不进行不安全的盲目重试。
5. 任务 2 红块的 PICKUP_TRANSIT 已改为单一中转点 (-5.08, 2.70, pi/2)，定向测试成功。
6. B 区停车位已从贴近木墙的旧位置调整为 (-2.75, -5.00, 0)。蓝块 1 已完成进入 B 区、放置与后续离场的回归，未再发生把已放物块作为离场障碍而卡住的问题。
7. 任务 3 蓝块 1 的完整抓取、运输、B 区放置及下一阶段 Nav2 离场已在日志中完成验证。B 区物块的障碍恢复策略改为：提交放置后立即恢复给后续规划使用，当前离场阶段则在机器人距离物块超过 0.70 m 前排除该物块，避免起步时自阻塞。

### 已验证的放置质量证据

- 早期异常证据：实际槽位误差约 0.425 m，竖直速度约 1.909 m/s；prepare 阶段还出现过 z=0.1548 m、竖直速度约 1.6339 m/s。这些均已纳入拒绝释放条件。
- 成功放置样本：物块实际位置约 (-0.121, 0.124, 0.004)，竖直速度约 0.0012 m/s，抖动速度约 0.00077 m/s，满足水平、静止和槽内的门控。

### 已部署、但尚未回归验证的改动

1. 任务 2 的 DROPOFF_TRANSIT 近点交接

   - 日志 204238 显示 6 次尝试都在机器人约 (-5.34, 3.78) 时失败，而目标约为 (-5.35, 3.90)，平面误差约 0.12 至 0.13 m。
   - 根因是近点成功判定只覆盖 PICKUP_TRANSIT，未覆盖 DROPOFF_TRANSIT；并非“不支持斜线”。
   - 已部署最小改动：DROPOFF_TRANSIT 在平面距离小于等于 0.15 m 时取消朝向约束，记为 SUCCEEDED_PROXIMITY，并进入 1 s 的交接等待。
   - 该改动已构建并部署，但因本次明确停止任务，尚未跑验证。恢复后的第一项测试必须只验证这一处。

2. 蓝块 2 的 B 区离场中转

   - 任务 3 后的离场曾在约 (-2.46, -4.57) 附近受真实木墙转角影响卡住；直接前往西北方向目标时局部规划不可行。
   - 手工 Nav2 验证表明向东绕至约 (-1.68, -4.39) 后可稳定离开，且与已放蓝块距离超过 1 m。
   - 已为 blue_cube_2 部署 pickup_transit 中转点 (-1.80, -4.40, 0)。
   - 该中转尚未完成完整的任务 4/任务 5 回归，不能标记为已验收。

### 待完成工作与推荐恢复顺序

1. 确认任务仍为 active=false，且不在任务执行中修改参数或重启。
2. 先运行“任务 2 携带红块的 DROPOFF_TRANSIT 到 A 区”单段测试，日志中应出现 Dropoff transit accepted 或等价的 SUCCEEDED_PROXIMITY；若仍失败，只检查近点判定、实际目标位姿和碰撞层。
3. 单独回归任务 3 的 B 区放置与离场，确认已放蓝块在距离大于 0.70 m 后恢复为障碍。
4. 单独执行任务 4，重点验证 blue_cube_2 经 (-1.80, -4.40, 0) 的离场和后续抓取，不与其他参数改动混合。
5. 单独执行任务 5，并逐次验收每个物块：水平、静止、落在 A/B 区内、无碰撞；最后一件应原地结算。
6. 仅在所有单段通过后运行完整五物块任务。运行中只记录，结束后再作一次单一、可归因的修改。
7. 最后在 CoStudio 人工验收 /mission/execution_status 驱动的 1/5 到 5/5、相机/识别/三维画面同步，以及 Windows localhost:9090 的 SSH 隧道持续可用。

### 关键文件与备份

- 任务与路径配置：/home/ros/dev_ws/src/moon_warehouse_coordinator/config/task_execution.yaml
- 状态机：/home/ros/dev_ws/src/moon_warehouse_coordinator/moon_warehouse_coordinator/mission_executor.py
- 机械臂与放置配置：/home/ros/dev_ws/src/moon_manipulation/config/fixed_manipulation.yaml
- 机械臂执行逻辑：/home/ros/dev_ws/src/moon_manipulation/moon_manipulation/fixed_manipulation_node.py
- 物块物理插件：/home/ros/dev_ws/src/moon_warehouse_gazebo_plugins/src/CubeCarryPlugin.cpp
- 已保留的变更前备份包括：pre_task1_rail_gate、pre_release_guard、pre_prepare_release、pre_3d_held_slot、pre_b_nav2、pre_blue2、pre_correction、pre_dropoff_transit 等命名版本；恢复时不得覆盖或删除。

### 近期日志索引

- 任务 1 门控与抓取：competition_20260901_192905.log、competition_20260901_193253.log、competition_20260901_193846.log
- A 区放置与任务 3 成功链路：competition_20260901_191254.log、competition_20260901_194221.log、competition_20260901_201028.log
- 任务 2 抓取中转成功：competition_20260901_191851.log
- DROPOFF_TRANSIT 近点失败证据：competition_20260901_204238.log
- 最新一次启动记录（未作回归）：competition_20260901_205458.log

### 仍需遵守的约束

- 一次只改一个变量；任务 active 时绝不改参数或重启。
- 不恢复高频 ROS 服务式物块跟随。
- 不以缩小全局安全距离强行通行；小物块半径如需调整，必须独立验证并记录证据。
- 不将局部失败笼统归因于“不支持斜线”；先以位姿、代价地图、MPPI、Collision Monitor 和恢复日志区分可达性、局部规划、振荡与让行问题。
- 本文此前较早章节为历史记录；与本节冲突时，以本节为准。

## 当前已停止的状态

- `moon-warehouse.service` 仍在运行，但任务流已停止：`/mission/execution_status` 为 `STOPPED`，`active: false`。
- 未有导航、抓取或放置任务处于 active 状态；本记录之后未再修改代码、参数、地图或世界文件。
- `/home/ros` 所在分区已满：39 GiB 中已用约 37 GiB，仅余 63 MiB（系统报告为 `100%`）。
- 最后一次新建备份在空间耗尽时中断，目录 `/home/ros/20260901_a_zone_release_slot_y_pre` 约 697 MiB，**是不完整副本，不能作为恢复备份**。此前已有的完整备份均未删除。
- 最后一次 A 区槽位配置改动只同步到了工作区源码：`/home/ros/dev_ws/src/arm_action_integration/config/fixed_manipulation.yaml`；构建因磁盘满失败，服务没有重启，因此运行中的 install 配置并未切换到这项改动。Windows 暂存副本 `_u22_patch/multi_fix/fixed_manipulation.yaml` 也含该未验证改动。

## 已完成并保留的工作

### 1. 启动、连接和备份

- 已建立 Windows 到 `u22` 的 SSH 工作链路。
- 已将初始代码另存为 `/home/ros/dev_ws9.1`，并保留多份阶段性备份；后续不得删除这些有效备份。
- 一键启动链路已带 RViz：`run_competition.sh` 使用 `start_rviz:=true`。RViz 偶发 GLSL 链接报错不影响导航栈激活。

### 2. 上半区移动障碍物（冻结，不再改动）

`moving_obstacle_1` 已固定为：

- 运动端点：`(-3, 3)` 与 `(2, 3)`；
- 巡航速度：`0.20 m/s`；
- 已禁用所有机器人触发的停车、退让与方向锁存；障碍物只按自身端点反向；
- 日志连续确认其在两端自动反向，没有卡壳。端点附近短暂减速用于制动和反向，不是停滞。

用户已明确要求它不是协作式障碍物，必须持续巡逻且不主动避让机器人；后续不再调整其路径、速度和非协作规则。此项为冻结基线。

### 3. 地图与障碍物核查

- 已核查用户截图中上侧大面积代价：并非虚假障碍；静态地图与 Gazebo 世界一致，`y≈4.93` 存在真实连续墙体。
- 外绕高位路线曾在 `(3.27, 4.59)` 一带失败，全球代价 `186` 来自该真实墙体的膨胀，不能通过“清地图”消除。
- 小物块虚拟层的 `mark_radius=0.045 m` 在 `0.05 m` 分辨率下已近似单栅格；未缩小全局安全距离，也未随意删除真实墙、方块或石块障碍。
- 因此，“局部失败不是因为不支持斜线”，也不应以删掉真实地图障碍或缩小全局安全距离强行通行。

### 4. 抓取、随动与放置机制

- 保留物理循环内的 `CubeCarryPlugin` 方块随动；没有恢复高频 ROS 服务式物块跟随。
- 抓取后，方块先以工具端随动，再以底盘随动运输；放置时切回工具端随动，张爪后物理释放。
- 放置验收已包含：接地高度、平面/竖向抖动、线速度、区域边界、与已放方块净距、最终水平与静止检查。
- `placement_drop_height` 已由较高释放高度降为 `0.005 m`，目标是避免悬空落下和横向弹跳；成功样本显示接地后平面抖动约微米量级。

### 5. 任务 1 `PICKUP_PRE` 的定向验证

连续两次干净场景运行均成功进入精确对接并完成红块 1 抓取：

| 日志 | 前置段结果 | 精确对接/抓取结果 |
|---|---|---|
| `competition_20260901_184449.log` | 距离目标 `0.316 m` 时交接 | 抓取窗口误差 `0.0211 m`，物理抓取成功 |
| `competition_20260901_184942.log` | 距离目标 `0.430 m` 时交接 | `PICKUP_REACHED`、`OBJECT_GRASPED`，方块底盘随动成功 |

这说明当前直接 `pickup_pre` 路径在本轮两次试验中没有出现 `STALLED_NO_PROGRESS`，但尚不足以宣称其已达到多轮稳定性。

### 6. 已保留、但尚未完成验收的路径尝试结论

- 红块 1 低位中转、两段低位中转与上侧外绕均已试验；低位会碰真实下方墙或方块列，上侧外绕被 `y≈4.93` 的真实墙体及其膨胀区阻断。
- 这些失败路线已从当前红块 1 配置中回滚；当前红块 1 使用直接 `pickup_pre`。
- 红块 2 保留三段抓取中转能力（先向西、再向上、后横向接近），但还没有在本暂停前完成验收。

## 本轮已发现、尚待修复的问题

### A. A 区物理放置越界（当前最明确的功能问题）

本次红块 1 已成功携带到 A 区。第一轮运输 45.5 秒无有效进展后，系统清理代价地图并重试；第二轮成功到达放置交接门，导航残差 `0.121 m`。

放置时的真实证据：

```text
Dynamic place red_cube_1: zone_a slot 2, local=(-0.060,0.180), IK residual=0.0064m
held-slot alignment: actual=(-0.059,0.189), target=(-0.060,0.180), error=0.0087m
physical release: xy=(-6.8792,-2.7167), z=0.0179m,
  floor target=0.0150m, planar_jitter=0.000002m,
  vertical_jitter=0.000118m, max_speed=0.001156m/s,
  grounded=True, stable=True
place red_cube_1 rejected: red_cube_1 settled outside zone_a: local=(0.121,0.283)
```

结论：释放前方块已对准目标槽（误差仅 8.7 mm），释放后却被夹爪/接触解算推到 A 区外。A 区允许的中心点范围是 `x=±0.475 m`、`y=±0.225 m`，实际 `y=0.283 m` 因而被正确拒绝。它不是“悬空”或“严重抖动”：物块已接地、静止，但位置不合格。

已提出但**未部署、未验证**的单变量思路：将 A 区五个槽位统一移至 `y=0.10`，给释放后的约 `+0.094 m` 纵向接触偏移留下边界余量；不放宽区域边界、不放宽物理验收。此变更仅在源码/Windows 暂存文件中，受磁盘满影响未构建。

更稳妥的后续验证应先单独记录“释放前 zone 坐标 → 释放后 zone 坐标”的多次偏移，再决定是否仅采用该槽位布局，或需要处理夹爪碰撞几何与释放时序。不能通过放宽 `placement_boundary_margin` 来掩盖问题。

### B. 红块携带至 A 区的导航存在一次恢复

同一日志中的第一条 `DROPOFF` 路径在 45.5 秒内未达到 `0.20 m` 的最佳距离改进阈值：

```text
Smoothed path leads to a collision at x: -3.400000, y: -0.200000, theta: -1.029869
Navigation goal-progress watchdog: best remaining distance did not improve by 0.20 m for 45.5s
Navigation recovery cleared local/global costmap(s)
RETRY_WAIT: DROPOFF leg STALLED_NO_PROGRESS; retrying attempt 2
```

第二次重试随即成功到达。因此不能把它归因于“不支持斜线”；需要在恢复前后采集机器人位姿、动态障碍位姿、MPPI、局部/全局代价地图和 Collision Monitor 状态，才能决定是否做最小路径或控制器修改。

### C. `PICKUP_PRE` 仍需多轮稳定性验证

历史日志中，红块 1 的直接前置段曾在约 `(-4.4, 3.57)` 附近连续无进展；本轮两次则成功。潜在因素包括移动障碍物时序、局部规划振荡、时间戳外推以及动态地图更新。当前还缺少同配置下的重复统计，不能宣称问题完全消失。

本轮还观察到少量时间戳相关告警：

```text
Message Filter dropping message: frame 'laser_link' ... timestamp earlier than all data in transform cache
transformPoseInTargetFrame: Extrapolation Error ... requested time slightly ahead of latest transform
```

这些告警需要与停滞时间点对齐后再判断是否为根因，不能先改控制参数。

### D. 磁盘空间阻断了安全构建和备份

最新尝试先创建备份、再编译，但因根分区已满失败：

```text
cp: error writing ...: No space left on device
OSError: [Errno 28] No space left on device: 'log/build_2026-09-01_18-54-59'
```

后续恢复工作前必须先由用户决定如何释放或扩容空间。有效备份不可删除；新产生的 697 MiB 不完整目录不是可用备份，但是否删除也应先经确认。空间恢复前不得继续 build、创建完整工作区副本或部署变更。

> 2026-09-02 更新：该阻断已解除。已清理重复完整工作区和旧散落备份，仅保留两份有效 VM 基线；根分区恢复至约 7.1 GiB 可用。当前及后续备份改为 GitHub 分支 `codex/2026-09-02-regression` 的提交，不再创建完整工作区副本。

## 2026-09-02 回归与修复更新

### 1. B 区物块被弹飞：根因与修复

日志 `competition_20260902_105951.log` 证明 `blue_cube_1`、`blue_cube_2` 释放时均已接地、水平且静止。真正的问题在放置后的“固定”实现：`LinkAttacher` 在当前仿真中只有一个全局附着槽。放下一块后，下一次放置调用 `_detach_stale()` 会解除上一块的 `zone_b ↔ cube` 关节，使上一块重新成为自由刚体，随后可能被底盘或 Nav2 恢复旋转撞飞。

修复提交：`ccc67ab Keep all verified placements physically secured`。

- `CubeCarryPlugin` 新增多物块 `secure:<cube>` / `unsecure:<cube>` 锁存集合；
- 每块只锁存在它通过物理落地验收后的 Gazebo 真值位姿，不吸附到名义槽位；
- 保持工作发生在 Gazebo 物理循环内，没有恢复高频 ROS 服务式跟随；
- 放置服务等待 `secured:<cube>` 确认，不再使用单槽 LinkAttacher 固定已放物块。

定向扰动测试同时锁存两块蓝块，分别强制施加 `(5,5,1)+5 m/s` 与 `(4,4,1)+5 m/s` 扰动；1 秒后两块均回到各自锁存点且速度归零。

完整流程日志 `competition_20260902_115536.log` 中，锁存计数依次为：

```text
red_cube_1 -> total=1
red_cube_2 -> total=2
blue_cube_1 -> total=3
```

`blue_cube_1` 释放坐标为 `(-2.606938,-5.089399)`。任务 3 完成后又经历 B 区离场和任务 4 六轮 Nav2 规划/恢复，结束时 Gazebo 真值仍为 `(-2.606938,-5.089399)`，姿态水平、速度近零。B 区“上一块被下一次操作解绑并弹飞”的问题已修复。

### 2. 本轮完整回归结果（3/5）

- 任务 1：`pickup_pre` 第一次到达并成功抓取；A 区运输第一次 `STALLED_NO_PROGRESS`，第二次成功；放置第一次因 carry 指令确认超时安全拒绝，重试后物理放置成功。
- 任务 2：`pickup_transit` 共到第 5 次才通过；`DROPOFF_TRANSIT` 在 `0.101 m` 接受；第一次放置因 IK residual `0.1174 m` 安全拒绝，重试成功。
- 任务 3：蓝块 1 抓取成功；到 B 区第 6 次导航尝试到达；物理放置、水平/静止/区域内验收成功，锁存计数达到 3。
- 任务 4：从 B 区经 `(-1.80,-4.40)` 离场成功，但 `pickup_pre (-5.08,1.70)` 六次均中止，流程以 `ERROR` 结束，完成数 3/5。

任务 4 的失败证据不是斜线能力问题。机器人停在约 `x=-2.5~-3.0, y=1.34~1.39`；平滑器反复报告路径在 `(-4.18,1.26)` 或 `(-4.01,1.27)` 碰撞。该位置距未抓取的 `blue_cube_3(-3.95,1.70)` 约 0.50 m，属于真实方块膨胀区。随后 MPPI `Optimizer fail to compute path`，倒车和旋转恢复均被 Collision Monitor 以 `Collision Ahead` 拒绝。

下一项单变量修复思路（尚未部署）：在 `blue_cube_2` 现有 B 区离场点 `(-1.80,-4.40)` 后增加已实测的中心通道点 `(-5.08,2.70)`，先从蓝块行上方横向通过，再沿 `x=-5.08` 下探到抓取前置点；不改障碍半径和全局安全距离。

### 3. 上半区移动障碍物最终冻结规则

提交 `cd32239 Make upper obstacle patrol non-cooperative` 已部署并验证：

- 轨迹：`(-3,3) ↔ (2,3)`；
- 巡航速度：`0.20 m/s`；
- `stop_distance=0.0`，`yield_pass_lock_enabled=false`；
- 不因机器人靠近停车、退离、换向或侧移；机器人必须自行通过 SIPP/Nav2/Collision Monitor 避让；
- 日志 `competition_20260902_121808.log` 已记录一整次双向往返，始终 `y=3.0`，两端正常反向，无任何 `Yield retreat`。

该规则按用户要求冻结，后续不得再修改移动障碍物 1。

## 尚未完成的验收清单

1. 在同一冻结的上半区障碍设置下，重复任务 1 `pickup_pre` 定向测试，记录成功率、时长、恢复次数和停滞证据。
2. 修复并验证红块携带 `DROPOFF_TRANSIT → A 区`：斜向中转、夹持随动、净距、接地、水平、静止、区域内与不碰撞已放物块。
3. 单独验证红块 2 的抓取中转及其携带路线；现有三段 `pickup_transits` 尚未完成验证。
4. 单独验证蓝块到 B 区西侧停车位 `(-3.04, -5.00, 0)` 的可达性、机械臂放置和木墙净距。
5. 验收五次放置与最后一件原地结算。
6. 在所有单段通过后才运行完整五物块流程；运行中只记录，结束后一次只做一个可归因修改。
7. 在 CoStudio 人工确认 `/mission/execution_status` 的 `1/5 → 5/5`、相机/识别/三维画面同步，并检查 Windows `localhost:9090` SSH 隧道持续可用。
8. 将最终参数、日志证据、各段耗时、恢复次数和未解决项回写至 `8.31md.md` 及运行记录。

## 恢复工作时的推荐顺序

1. 先处理磁盘空间：保留全部有效备份；明确处置不完整目录或增加磁盘空间，至少预留数 GiB 给备份、构建与日志。
2. 检查并统一源码与 install 配置：决定是回退未部署的 A 区槽位改动，还是在有空间、有效备份存在的前提下正式构建部署。
3. 做一次只针对 A 区物理释放的验证，记录 release 前后方块在 `zone_a` 坐标系中的偏移；边界与静止验收必须保持严格。
4. 再做多轮任务 1 `pickup_pre`，只收集证据；不改已冻结的上半区障碍物。
5. 按“红块携带至 A 区 → 红块 2 → 蓝块 B 区 → 五物块全流程 → CoStudio/隧道”的顺序推进。

## 关键日志索引

- `/home/ros/dev_ws/logs/competition_20260901_184449.log`：任务 1 前置段成功、精确抓取成功。
- `/home/ros/dev_ws/logs/competition_20260901_184942.log`：第二次前置段成功；红块携带至 A 区第一次停滞后恢复；物理释放接地静止但越界拒绝。
- `/home/ros/dev_ws/logs/competition_20260901_182642.log`：历史直接 `PICKUP_PRE` 多次停滞证据。
- `/home/ros/dev_ws/logs/competition_20260901_181932.log`：较早的物理放置成功样本，方块已接地并成功固定在 A 区。
- `/home/ros/dev_ws/logs/competition_20260831_214444.log`：用户指定的最初 `PICKUP_PRE` 连续 `STALLED_NO_PROGRESS` 原始证据日志。

## 必须持续遵守的约束

- 保留有效备份；一次只改一个变量；任务 active 时不改参数、不重启。
- 不恢复高频 ROS 服务式方块跟随。
- 不以缩小全局安全距离、删除真实障碍或放宽放置边界来强行通过。
- 不把局部失败简单归为“不支持斜线”。
- 上半区移动障碍物维持 `(-3,3) ↔ (2,3)`、`0.20 m/s`，后续不再改动。

## 2026-09-02 最新同步（以本节为准）

### GitHub 备份状态

- 今日全部已完成代码修改已提交并同步到 GitHub 仓库 `Xiaolidw/algorithm-dev_ws` 的分支 `codex/2026-09-02-regression`。
- 已确认远端分支 HEAD 为 `5f23fe1ef3c4c463867aa95ad05257878cff1fc7`；本机 Git 工作树在同步前为干净状态。
- 今天关键提交链：
  - `ccc67ab`：用 Gazebo 物理循环内的多物块 secure 锁存替代单槽 LinkAttacher，解决 B 区已放方块被后续放置解绑、弹飞的问题。
  - `cd32239`：冻结上半区非协作移动障碍物为 `(-3,3) ↔ (2,3)`、`0.20 m/s`，永不因机器人让行。
  - `d2a0e60`：地图生命周期与 `/map` 就绪门控。
  - `13904e2`：任务 4 经中心通道的抓取路线。
  - `6219104`：恢复经实测的 B 区交接容差。
  - `0bc6f39`：B 区槽位改为 12 cm 间距。
  - `3db1c64`：最后一个蓝块使用独立 B 区接近点 `(-2.60,-4.60,0)`，避开已占用的旧公共停车位。
  - `e094db6`：A 区红块槽位改为 `(-0.18,0.12)`、`(-0.06,0.12)`。
  - `5f23fe1`：移除放置动作内部的四轮三维纠偏，改为“单次真实位姿核验 + 既有任务级重新放置”。

### 最新放置问题、证据与最小修复

本轮日志：`/home/ros/dev_ws/logs/competition_20260902_144752.log`。

- 任务 1 已完成抓取；前往 A 区第一次出现 `DROPOFF STALLED_NO_PROGRESS`，任务级第 2 次导航随即到达，不在任务执行中修改导航参数。
- 原放置逻辑的第 1 次尝试实际出现明显上下振荡：物块相对 A 区高度依次约为 `0.116 → 0.093 → 0.180 m`，第 2、3 次尝试也出现 `0.105 → 0.131 → 0.154 m`、`0.125 → 0.172 → 0.124 m` 的反复变化。
- 失败不是槽位初始 IK 不可达：槽位 `(-0.180,0.120)` 初始 IK residual 为 `0.0015 m`；失败发生在后续把实际物块误差再次累加到夹爪中心 IK 目标后，最终 correction residual 达 `0.0738 m` 并被安全门控拒绝。
- 该行为正对应“放置时悬停、抖动严重”的观察。修复 `5f23fe1` 保留原有严格释放要求（区域内、接地高度、平面误差和竖向误差），但不再在同一次放置中执行最多四次机械臂纠偏；首次放置后的真实物块若不在窗口内，立即拒绝释放并返回 home，由已有任务级重试从新的稳定初始姿态重新执行。
- 修改已通过 Python AST 语法检查、Git 空白检查、`moon_warehouse_moveit` 构建，并已部署到 `/home/ros/dev_ws`。重启后的 arm、gripper、joint_state 控制器均为 `active`，Nav2 和地图均已激活。

### 本轮验证状态

- 已启动一次应用 `5f23fe1` 后的完整回归，用于验证“不再抖动”的放置行为以及 A/B 区新槽位与最后蓝块独立 B 区停车位。
- 启动时曾出现 gripper_controller 已加载但 `unconfigured` 的竞态；任务尚为 IDLE 时已通过 controller_manager 的 configure/switch 正常恢复为 `active`。这是启动时序问题，不是放置或导航逻辑问题。
- 随后与 `u22` 的 SSH 连接在一次只读状态查询时被远端关闭（`Connection closed by 192.168.64.128 port 22`）。在该断开后未修改虚拟机参数、未重启服务、未中断仿真。恢复连接后应先读取 `/mission/execution_status` 和该日志尾部，确认任务是否仍 active；若 active，只记录到结束。

### 下一步（严格按此顺序）

1. 恢复 SSH 后确认任务状态；如果仍 active，绝不改动，先收集 `competition_20260902_145821.log` 的完整运行证据。
2. 验证任务 1 A 区放置：应只出现一次 `single-pass held-slot verification`，或一次安全失败后任务级重新放置；不得再出现连续 `held-slot alignment 0/4 ... 3/4` 的上下纠偏。
3. 若任务 1 通过，继续按完整回归证据验收红块 2、蓝块 1/2 的槽位间距和锁存稳定性。
4. 重点核验任务 5：导航目标应使用 `blue_cube_3` 的独立位置 `(-2.60,-4.60,0)`；如果到达后第三个 B 槽位仍不适合，只能在整轮结束后做一项基于日志的单变量改动。
5. 只有整轮结束后再处理任务 1 的一次 `DROPOFF STALLED_NO_PROGRESS`，提取机器人/动态障碍、局部全局代价地图、MPPI、Collision Monitor 与恢复证据；不得在没有证据时缩小全局安全距离或调整已冻结的上半区障碍物。

## 2026-09-04：B 区停车/放置优先优化（以本节为准）

### 范围与约束

- 本轮只改 B 区蓝块的底盘停车交接链路；没有启动、修改或验收任何 A 区放置任务。
- 未改全局安全距离、物块半径、真实木墙、上半区移动障碍物轨迹/速度/非协作规则，也没有恢复高频 ROS 服务式物块跟随。
- u22 修改前为 `IDLE`；构建和服务重启均在无 active 任务时执行。

### 已部署的最小改动

GitHub 分支 `codex/2026-09-02-regression` 已推送提交：`661a16e feat: stage B zone parking before placement`。

旧 B 区流程是“Nav2 直达终端停车位，最大允许 0.32m 残差后立即调用机械臂”。新流程只对 `blue_cube_1/2/3 → B` 生效：

1. Nav2/A* 先去 B 终端位东侧约 0.50m 的预接近点；蓝块 1/2 为 `(-2.25,-5.00,pi)`，蓝块 3 为 `(-2.10,-4.60,pi)`。
2. 进入 `0.16m` 预接近容差即取消 Nav2 的末端航向细调，避免在木墙附近端点振荡；等待取消交接完成后，连续零速 `0.60s`。
3. 通过安全速度链 `/cmd_vel_nav_raw → Collision Monitor` 沿最终直线走廊低速驶入，远段上限 `0.10m/s`。
4. 到最终目标约 `0.20m` 时再次连续零速 `0.50s`。
5. 继续以最大 `0.045m/s` 低速进入；到 `0.12m` 以内后再对齐机械臂所需的最终航向。
6. 航向合格后连续零速 `0.70s`，重新严格检查残差与航向，才调用既有物理放置与释放验收。

若 Collision Monitor 保持底盘、4 秒无距离改善或总计超过 35 秒，流程会拒绝放置并从完整 B 区预接近重新规划；不会直接重试穿越木墙终端走廊。

### 已完成验证与证据

- 新代码已通过 AST/语法和 Git 空白检查；`moon_warehouse_coordinator` 已在 u22 构建成功并部署。
- 服务重启后 `/mission/execution_status` 为 `IDLE`；Nav2、arm_controller、gripper_controller 和 joint_state_broadcaster 均为 `active`。
- 单独 B 区预接近验证日志：`/home/ros/dev_ws/logs/competition_20260904_125249.log`。
  - 从初始位姿仅向 `/navigate_to_pose` 发送 `(-2.25,-5.00,pi)`，没有抓取、放置或 A 区任务。
  - 约 18 秒后动作 `SUCCEEDED`，恢复次数 `0`。
  - 最终 `/odom` 为约 `(-2.233,-5.063)`，距离预接近目标约 `0.065m`，满足新 `0.16m` 的接管门槛。

### 尚未验收的部分

- 本次没有携带蓝块，因此尚未验证实际载物时的两次稳定等待、低速末段、最终 `0.12m` 停车、机械臂释放、B 区多物块净距和 secure 锁存稳定性。
- 下次优先运行 B-only 携带/放置验证；如只能运行完整任务，A 区阶段仅作为前置记录，不在 active 时改动，等到 B 区完成后再根据单一证据决定下一项改动。

### 结束状态

- B-only 预接近测试后已在无 active 任务时重启 u22 服务，最新启动日志为 `/home/ros/dev_ws/logs/competition_20260904_125601.log`。
- 复位后服务为 `active`，`/mission/execution_status` 为 `IDLE`；后续可从干净初始场景开始 B 区载物/放置验证。

## 2026-09-04：避障碰撞诊断与轨道保持修复（以本节为准）

### 已确认的碰撞根因

- 日志：`/home/ros/dev_ws/logs/competition_20260904_160121.log`。
- `PICKUP_PRE` 期间，Gazebo 真值采样到机器人位于约 `(-1.820, 2.736)`，移动障碍物 1 位于约 `(-2.262, 3.084)`，两者中心距约 `0.56m`，进入其 `0.60m` 实体碰撞体的接触区。
- 障碍物 1 的真实速度从规定的 `0.20m/s` 降至约 `0.001m/s`，并偏离 `y=3` 轨道；但预测器仍按已发布的巡逻假设规划。Collision Monitor 当时只执行 `approach`（减速），未停车，MPPI 随后反复 `Optimizer fail to compute path` / `STALLED_NO_PROGRESS`，恢复行为亦报告 `Collision Ahead`。

### 已部署的最小修复

- 仅对上半区 `moving_obstacle_1` 增加确定性轨道锁：保留实体碰撞体、固定路线 `(-3,3) ↔ (2,3)` 与速度 `0.20m/s`，但不再允许底盘接触把它推偏、卡住或改变方向；它仍不对机器人让行。
- Collision Monitor 的同一预测风险区从 `approach` 改为 `stop`：预计发生接触即底盘停车，待障碍物通过后继续，未缩小全局安全距离或删除任何真实障碍。
- 空闲场景真值采样（6 秒）：障碍物 1 从 `x=-2.202` 连续运行至 `x=-0.722`，`y_span=0.0000m`，速度范围恒为 `[0.200,0.200]m/s`。构建通过：`gazebo_physics_obstacle_plugin`、`mybot_description`、`moon_warehouse_bringup`。

### 同轮 B 放置证据与待解决项

- 已接入 MoveIt FK/Cartesian 预放置对位与单次垂直下放；真实 B 槽位平面改用未叠加手写 IK 的 `-0.05m` 标定偏置。
- 最新 B-only 放置中，安全高度对位路径可完整生成；最终垂直路径完成比例由旧版的 `0.20~0.38` 提升至 `0.625 → 0.750 → 0.833`，但仍未达严格的 `1.000`，因此安全拒绝松爪，没有发生物块弹飞或虚假放置。
- 下一步：在新轨道锁与预测停车生效后，先单独复测 `PICKUP_PRE` 的无接触通过率；再基于 B 末端真实 FK/可达边界，调整唯一的底盘工作位或槽位高度参数，不能以接受不完整笛卡尔路径、放宽释放校验或删除放置动作来通过。

## 2026-09-04：精确通道安全边界与 B 区放置回归（最新）

- 真实底盘为 `0.32 × 0.32m`，轮组横向外沿为 `±0.210m`。两张代价地图的足迹从 `±0.24m` 收至 `±0.22m`，仍有每侧 `10mm` 物理余量；前向 `0.48m` 的机械臂/载物包络不变。墙体、物块、激光和 Collision Monitor 的硬碰撞约束均未缩小。
- 发现 Humble Collision Monitor 在 `stop` 模式下仅配置 `footprint_topic` 会因 `FootprintApproach.points` 缺失而无法启动。现补入同一足迹的扁平点数组；日志 `competition_20260904_162019.log` 后，导航生命周期确认 `Managed nodes are active`，恢复了硬制动链。
- 为缩小纯规划冗余，全局 inflation 仅从 `0.70m` 调为 `0.45m`；局部仍保留 `0.50m`。因此在物理横向半宽外仍有至少 `0.23m` 全局软缓冲，并由局部足迹碰撞检查和 Collision Monitor 双重否决。
- B 区失败证据：`competition_20260904_162019.log` 中，旧终端附近底盘约 `(-2.227,-4.989)`，放置工具前伸约 `0.374m`，垂直笛卡尔段只完成 `0.600 → 0.708 → 0.792`，全部安全拒绝松爪。
- 单变量修复：B 终端基准由 `(-2.20,-5.00,pi)` 向西改为 `(-2.30,-5.00,pi)`，降低末端前伸而不靠近木墙到危险范围；`(-2.0,-5)` 会使前伸额外增加约 `0.20m`，并不具备可达性优势。
- 成功验证日志：`/home/ros/dev_ws/logs/competition_20260904_162633.log`。B-only `blue_cube_1` 回归最终停车通过（残差 `0.095m`、中途/最终零速停稳均执行）；上方对位与单次垂直下降均为 `100%`。释放真值：`(-2.4999,-5.1180,0.0152)`，平面误差 `0.0020m`、高度误差 `0.0017m`、最大速度 `0.001161m/s`，物块水平、静止、在 `zone_b` 内并已 secure；任务单段 `FLOW_COMPLETED`。
- 该成功轮的 B 预接近第一次曾 `STALLED_NO_PROGRESS`，使用既有新鲜预测重试一次后到达；此问题与 B 终端/放置无关，后续应单独提取该段动态障碍和局部规划证据，不在 active 任务中调参。
- 将 `0.45m` 全局膨胀部署后，第三次 B-only 回归日志 `competition_20260904_163151.log` 同样 `FLOW_COMPLETED`，确认紧凑全局通道偏好没有破坏 B 区停车与放置。当前服务处于非 active 的完成状态。

## 2026-09-04：B 区正向伸臂放置唯一化与复验（最新）

### 旧逻辑清理

- 发现新 YAML 已使用 B 区纵向中心线槽位，但 Python 内置默认仍保留旧的五个侧向槽位；缺失参数文件或换启动入口时会回退到旧斜向放置。现已把内置默认与 YAML 统一为 `zone_b: (-0.09,0.00) → (0.00,0.00) → (0.09,0.00)`，按远到近放置，横向始终为 0。
- 删除运行配置中 `blue_cube_3 → (-2.60,-4.60,0)` 的反向停车覆盖，并把其 B 预接近点统一为 `(-1.55,-5.00,pi)`。三个蓝块现在均使用同一东侧预接近、`(-2.30,-5.00,pi)` 西向停车和正前方伸臂链路，不再存在蓝块 3 的 180° 终端转向/侧向伸臂分支。
- B 最终航向门限独立收紧为 `0.03rad`，避免用肩关节侧摆补偿底盘偏航；几何单次核验容差仅由 `0.035m` 放至 `0.040m`。区域边界、接地、水平、静止、物块间距和 secure 验收均未放宽。
- 活动源码扫描确认旧 B 槽位值和旧蓝块 3 停车点为 0 处；历史备份仍仅作为 GitHub 快照，不参与构建和运行。

### 两次成功证据

1. 修改中心线槽位后的首轮日志：`/home/ros/dev_ws/logs/competition_20260904_165437.log`。
   - 动态放置明确选择 `zone_b slot 1 local=(-0.090,0.000)`，IK residual `0.0015m`。
   - MoveIt 上方对位和单次垂直下降均为 `100%`；释放点约 `(-2.59005,-4.99939,0.01578)`。
   - 单次核验平面误差 `0.0006m`、竖向误差 `0.0008m`；释放后速度 `0`，grounded/stable 均为 True，最终 `FLOW_COMPLETED`。
2. 清除所有旧运行分支并重启后的干净复验日志：`/home/ros/dev_ws/logs/competition_20260904_165926.log`。
   - 运行节点实载槽位为 `[-0.09/0, 0/0, +0.09/0]`；B-only 服务确认未排入 A 区任务。
   - 动态目标为 `cube_target=(0.382,0.021,-0.022)`，横向 2.1cm 仅来自最终停车实测残差，不是侧向槽位；两段 Cartesian 再次均为 `100%`。
   - 释放点约 `(-2.59004,-4.99945,0.01568)`；平面误差 `0.0006m`，释放最大速度 `0.001162m/s`，grounded/stable/secure 全部通过，最终 `FLOW_COMPLETED`。

### 保留的独立待办

- 第二次复验的载物 `B_DROPOFF_PRE` 第一次出现 MPPI optimizer failure、一次 `Collision Ahead` 和 `STALLED_NO_PROGRESS`，既有第 2 次新鲜预测重试后成功抵达。该现象属于载物导航恢复，不是放置代码未调用或放置失败；后续必须单独提取动态障碍、代价地图和控制器证据后再做单一修改。

## 2026-09-04：连续两个蓝块 B 区全链路回归（最新）

### 测试入口与范围

- 新增 `/mission/run_two_b_dropoff_regression`，固定连续队列为 `blue_cube_1 → blue_cube_2`，共 2 项且明确不生成 A 区任务；单块入口与正式完整任务共用同一抓取、导航、B 分段停车、物理放置和 secure 代码。
- 测试日志：`/home/ros/dev_ws/logs/competition_20260904_171154.log`。总耗时约 `271.9s`（4 分 31.9 秒），最终 `FLOW_COMPLETED: Completed 2 pick/place task(s)`。

### 分段结果

1. `blue_cube_1`
   - `pickup_pre`、物理 docking、抓取、载物前往 B 均为第 1 次到达；抓取后相对夹爪抖动为 0。
   - 放入首槽 `(-0.09,0)`；上方对位和垂直下降均 `100%`。单次核验平面/高度误差为 `0.0005/0.0007m`，释放最大速度 `0.001162m/s`，grounded/stable/secure 全通过。
   - 放置真值 `(-2.59004,-4.99947,约0.015m)`。底盘与方块已经物理分离约 `0.38m`，因此没有执行盲目固定距离倒车，而是由下一段 Nav2 经 `(-1.80,-4.40) → (-5.08,2.70)` 地图感知离场。
2. `blue_cube_2`
   - 第一块后的两个离场/中转点、`pickup_pre` 和 docking 全部第 1 次到达。动态抓取 IK 误差 `0.0007m`，夹爪窗口误差 `0.0217m`，抬升后相对抖动为 0。
   - 载物回到 B 的预接近和低速停车一次成功；选择第二槽 `(0,0)`，检测到 `occupied=1`。两段 Cartesian 均 `100%`，单次核验平面/高度误差 `0.0002/0.0008m`。
   - 释放真值 `(-2.49988,-4.99987,约0.015m)`，最大速度 `0.001161m/s`，grounded/stable/secure 全通过。作为最后一项，按既定要求原地停止并结算，不执行无意义退出。

### 结束后物理验收与异常审计

- 结束后再次读取 Gazebo 真值：两块姿态四元数均近似单位姿态，线/角速度近零；第一块在底盘离场、第二块抓取和再次进入 B 后仍保持 `(-2.59003,-4.99947)`，没有被推动或弹飞。
- 两块中心距约 `0.09015m`，扣除两个 3cm 方块后的表面净距约 `0.06015m`，高于 `0.055m` 物理净距门限；均完整位于 B 区。
- 本轮计数：`Optimizer fail=0`、`Collision Ahead=0`、`STALLED_NO_PROGRESS=0`、任务级恢复/重试=0。
- 平滑器有 3 次在 `(-2.85,2.95)`、`(-3.15,3.10)`、`(-4.70,2.85)` 判定平滑结果进入碰撞区；均位于上半区动态障碍轨道附近，约 0.2 秒后行为树把安全的未平滑路径交给控制器，所有对应导航仍 attempt 1 到达。这是安全拒绝和正确回退，不应通过放宽碰撞或强制平滑来“修复”。另有 4 次控制周期未达到 10Hz 和少量 TF 时间外推警告，未造成停滞或动作失败，后续只监测。
- 结论：当前连续两蓝块链路的抓取、载物避障、B 停车、中心线放置、第一块后的地图感知离场、多物块锁存和最后原地结算均通过；本轮不再修改导航或放置参数。

## 2026-09-05：固定障碍物锚定、最近取件与安全巡航提速（最新）

### 障碍物被撞飞的根因与修复

- 截图中的对象不是两条受控移动障碍物，而是世界模型 `Untitled` 内 13 个一米赛场障碍块。其模型原先为 `static=0`、各 link 质量 `1kg` 且没有关节；底盘接触可将它们推离静态地图中的原始位置，导致真实世界和代价地图不一致。
- 旧两蓝块日志 `/home/ros/dev_ws/logs/competition_20260904_171154.log` 的结束审计显示 12/13 个块偏移超过 5mm，最大 `1.2304m`；`link_4` 从约 `(-2.6955,-3.5429)` 被推到 `(-2.4372,-2.3399)` 并倾倒。这正是“障碍被撞飞”的直接证据。
- 已仅将该固定赛场复合模型锚定为 `static=1`，保留全部真实碰撞几何；移动障碍物 1 保持其用户规定的 `(-3,3) ↔ (2,3)`、`0.20m/s` 非避让运动。静态修复后的同一两蓝块流程审计为 13/13 块 `max_delta=0.0`。
- 锚定后暴露出原载物路线曾经依赖把 `link_4` 推开：旧路径在 `x≈-2.28` 向南进入其 `x=[-3.20,-2.20], y=[-4.04,-3.04]` 实体范围。蓝块到 B 区现统一先经固定块东侧的开阔中转点 `(-1.45,-2.40)`，再进入既有 B 预接近点；没有缩小安全距离或删除实体障碍。

### 调度、速度与放置超时清理

- 执行器现在每次启动下一次抓取前，依据实时底盘/未完成物块 Gazebo 位姿，将最近且可开始接近的任务换到当前队首；`red_cube_1` 在上半区轨道门未放行时仍视为暂不可达，不会阻塞其它可安全执行的最近任务。每个物块既有的中转、预接近、精密 docking、Collision Monitor 和 SIPP 门控不变。
- 巡航不再被旧 `0.60m/s` MPPI 上限及 `0.55m/s` 动态策略全局压低：开阔且对齐的 `FAST_CRUISE` 最高 `1.20m/s`、普通巡航 `0.95m/s`、谨慎模式 `0.45m/s`。SIPP 对预测冲突降速，Collision Monitor 仍持有最终停车权；抓取直线进给和 B 区分段停车继续使用独立低速控制。
- 修复一次放置动作结果丢失时的清理顺序：Gazebo 已证实物块接地后，先取消旧动作、发出一次 carry/base-lock 释放、等待 `1.5s` 让服务端收尾，再开始下一项。此前直接推进队列会让 `manipulation base lock` 留在锁定状态，后续底盘只能原地转向并累积 `STALLED_NO_PROGRESS`；该修复不删除或跳过放置动作。

### 最新验证

- 运行日志：`/home/ros/dev_ws/logs/competition_20260905_151718.log`，B-only `blue_cube_1` 生产路径 `FLOW_COMPLETED`。
- 抓取后载物经新的东侧中转、B 预接近和低速停车到位；无 `Optimizer fail`、`Collision Ahead` 或 `STALLED_NO_PROGRESS`。
- 放置完整返回 `place_complete`：物理释放真值约 `(-2.5900,-4.9995,0.0145)`，`grounded=True`、`stable=True`、最大速度 `0`；最终底盘锁明确释放，任务原地结算。
- 后续完整五物块回归应记录最近任务选择顺序、各巡航模式实际速度、每段耗时及恢复次数；运行 active 时仍禁止改参或重启。

## 2026-09-05：地图基准审计、单 RViz 与非最左蓝块回归

- 地图不再改动墙体、石块的坐标、尺寸或碰撞几何。将墙体父坐标临时改为 `0.9981` 后，截图确认会破坏石块与墙的相对布局，已回退该源码修改。
- 以旧地图 `competition_v2_scoring_u22.world` 的 `<state>` 为基准读取 Gazebo 真值：`Untitled` 13 块固定石头逐项最大坐标误差为 `0.000000m`；例如 `link_4=(-2.695500,-3.542920,0.500000)`，相邻 `Wall_75=(-3.262160,-4.855530,0)`。保留 `static=1` 仅防止这些固定赛场障碍物再次被撞飞。
- 一键任务启动原先同时启动导航 RViz 和 MoveIt RViz。现仅保留导航的 `competition_nav_topdown.rviz`；MoveIt 仍后台提供 `move_group`、抓取和放置服务。运行时进程审计确认仅 1 个 `rviz2`。
- 新增逐个回归入口 `/mission/run_next_nonleft_blue_b_dropoff_regression`，顺序为 `blue_cube_2 → 3 → 4 → 5`，每次先重置 Gazebo，避免 B 区槽位占用干扰。
- 地图基准下：`blue_cube_2` 成功抓取、经 B 分段停车并释放于 `(-2.5900,-4.9992,0.0149)`，`grounded/stable=True`、速度和抖动为 0；`blue_cube_4` 同样成功，释放于 `(-2.4998,-4.9999,0.0148)`。
- `blue_cube_3` 抓取成功，但载物 `DROPOFF_TRANSIT → (-1.45,-2.40)` 连续 6 次 `ABORTED`；Collision Monitor 拒绝 Spin/DriveOnHeading 恢复。该问题与地图坐标、抓取、放置无关，后续应只针对该载物局部恢复链取证和最小修复。
- `blue_cube_5` 在任务停止前可到达 pickup 预接近与最终 docking，但后者被 Collision Monitor 恢复拦截并多次超时；该验证为定位记录，未在 active 时更改参数。

## 2026-09-05：以最新 `offic_room.world` 恢复静态墙体基准

- 新基准文件：`C:\Users\奶龙\Documents\xwechat_files\wxid_oecmvo5kd6fd22_c1bc\msg\file\2026-09\offic_room.world`。它不同于此前比较的插件版世界文件，且明确规定 `officeroom` 父模型和 `<state>` 的原始位姿均为 `(0.9981, -4.92976, 0)`。
- 审计结果：31 个墙体 link 的内部几何、局部位姿、尺寸和碰撞形状与当前世界逐项相同；当前错误仅是父模型定义曾为 `(-0.693163, -4.92976, 0)`，会使整组墙体左移。已恢复为原始 `0.9981`，并恢复 31 处原始墙体接触定义 `<ode />`。
- 保留项：ABC 区、小物块坐标、两条移动障碍物轨迹均未改动；`Untitled` 的石块坐标不变，仍维持 `static=1`，防止再次被底盘撞飞。
- 已在 u22 重建 `mybot_description`，并以虚拟机 `DISPLAY=:0` 重启一套 Gazebo、Gazebo GUI 和 RViz；进程审计为各 1 个。重启遗留的 30 个旧 ROS 子进程已清理，避免同名节点/Fast DDS 端口冲突。最新启动日志：`/home/ros/dev_ws/logs/competition_20260905_175016.log`。

## 2026-09-06：13 个石块障碍层坐标系修复

- 两份 world 文件中 `Untitled` 父位姿、13 个 link 初始位姿和 `<state>` 位姿逐项完全一致；Gazebo 实体石块源码没有偏移。
- 根因位于 `cube_obstacle_map_node.py`：`fixed_obstacles` 误把 `Untitled` 的 link 局部坐标当作世界坐标，漏加父模型平移 `(0.968798, -5.71233)`，使 RViz/Nav2 中所有虚拟石块统一偏移 `(-0.968798, +5.71233)`。
- 已将 13 个坐标改为最新 `offic_room.world` 的绝对 `<state>` 真值；自动校验 13/13 一致，最大误差 `0.0m`。ABC 区、小物块、移动障碍物路径、墙体和实体石块均未修改。
- u22 已重建 `moon_warehouse_bringup`；运行节点确认加载 `10 cubes, 13 fixed stones, 3 placement zones`。新实例进程审计为 launch/Gazebo/Gazebo GUI/RViz 各 1 个，Gazebo 与 RViz 窗口均已在 `DISPLAY=:0` 可见。日志：`/home/ros/dev_ws/logs/competition_20260906_133902.log`。

## 2026-09-06：Gazebo “整体南移”复核与 GUI 视角复位

- 对比 `C:\Users\奶龙\Desktop\地图\地图\mybot_description\worlds\offic_room.world`、当前源码、安装空间和 Gazebo 运行时状态；墙体与 13 个石块的世界坐标没有整体南移。图示中心石块 `Untitled::link_2` 的运行坐标为 `(1.73333, 1.16425, 0.5)`，与原始 world 的绝对状态一致。
- 两张截图使用了不同相机：异常观感时 GUI 相机为 `(-0.2621, -1.3024, 5.8198)` 的近距离倾斜视角；原始 world 相机为 `(-6.4487, -19.2949, 53.1041)`。相机高度相差约 9 倍，透视与旋转导致了“向南偏移”的视觉误判。
- 任务状态确认 `IDLE / active=false` 后，完整重启仿真栈，使新 Gazebo GUI 重新加载原始相机；复核相机位置精确恢复为 `(-6.4487, -19.2949, 53.1041)`。当前仅 1 个 gzserver、1 个 gzclient、1 个 RViz。
- 本轮没有平移或修改墙壁、实体石块、ABC 区、小物块及移动障碍物路径，避免用相机差异反向污染地图真值。

## 2026-09-06：回退地图后 Git 夹取/放置恢复与实测

- 按要求停止修改地图并恢复上一版 world；源码与安装空间 `competition_v2_scoring.world` 的 SHA-256 均为 `d8078a1a2e9eb1ad371f3a11626cb1b74c56eb7faee4bd19a38f7b4de37e5ae2`。本轮未改变墙壁、石块、ABC 区、小物块坐标或移动障碍物轨迹。
- 对照 Git 可追溯版本 `2667a0f4` 的夹取/放置思想，在现有 LinkAttacher 架构内保留动态 IK、抓取前置位→单次下探、偏航感知对称夹爪闭合、夹取窗口与抬升后约束校验；没有导入会修改 world 的仓库插件。
- 修复当前 `_place()` 只松爪、不执行放置轨迹的问题：放置改为与抓取对称的 `move_to_preplace_pose → descend_to_place_pose → validate zone → open → detach → settle/validate → home`。失败时保持夹爪闭合和物块连接，不虚假放置。
- 首轮红块验证发现“预放置后 Carry health invalid”。根因是行驶期校验以物块相对底盘为基准，机械臂主动伸出后该相对位置必然变化。最小修复为：行驶前继续使用底盘相对携带校验；预放置和下放后改用物块相对 `link6` 的三采样静态漂移校验，阈值仍为严格的 `0.010m`。
- 成功日志：`/home/ros/dev_ws/logs/git_pick_place_red_to_a_validation_3.log` 与 `git_pick_place_manipulation_3.log`。`red_cube_5` 夹取 IK 误差 `0.0013m`，夹取后/抬升后漂移 `0.000162/0.000054m`；预放置/下放后漂移 `0.000248/0.002581m`，完整放置通过。最终相对 A 区 `(0.1468,0.0543)`，世界高度 `0.0149m`，线/角速度均为 0。
- 红块到 A 虽成功，但在剩余约 `3.94m` 处因 RPP `collision ahead` 停留并使用 5 次恢复；这是导航效率问题，不是夹取或放置失败。
- 发现定向测试的空载取件入口显式使用了无恢复的 `navigate_w_replanning_only_if_path_becomes_invalid.xml`，绕过当前 Nav2 已配置的带恢复树。已只删除该覆盖，让空载取件使用默认清图/等待/旋转/后退恢复逻辑，载物路线和地图均不变。
- 另修复一次物理弹射暴露的交接安全漏洞：制动距离估算不再直接信任任意大的异常里程计瞬时值，速度输入上限钳制为 `1.5m/s`，避免交接阈值从正常 `0.40m` 膨胀到 `8.904m`。随后完整重启仿真，清除已弹至 `(-140.55,12.42)` 的异常运行态。
- 干净蓝块→B 回归日志：`/home/ros/dev_ws/logs/git_pick_place_blue_to_b_validation_3.log`。最近物块选择为 `blue_cube_5`；空载导航、精对接、夹取、载物导航、两段式放置、松爪验收与机械臂回位全部成功，全程恢复次数 0。最终世界位姿约 `(-2.26737,-4.99021,0.01493)`，相对 B 区 `(0.23263,0.00979)`，线/角速度均为 0。
- 待办：当前替换后的工作区缺少旧仓库的低矮物块虚拟障碍层。不能直接照搬旧节点，因为其同时混入石块与旧 ABC 坐标。下一步应独立恢复“仅小物块”层：核心标记半径约 `0.045m`，由既有 inflation 提供车体净距；当前抓取目标和携带物必须临时排除，放置后待底盘离开再恢复。墙壁/石块实体及地图坐标不得修改。

### 仅小物块虚拟障碍层（已完成）

- 已从 Git 旧节点中只恢复低矮物块占用功能；运行参数为 `mark_radius=0.045m`、`fixed_obstacles=[]`、`zone_areas=[]`，明确不向代价地图注入墙壁、石块或 ABC 区。Gazebo world 和静态 PGM 均未修改。
- 局部代价地图插件顺序为 `cube_layer → obstacle_layer → inflation_layer`，全局为 `static_layer → cube_layer → inflation_layer`；方块核心接近实体尺寸，现有 inflation 和 `robot_radius=0.22m` 继续提供车体净距。
- 节点同时理解正式 `/mission/execution_status` 与定向测试 `/pick_place/navigation_status`。当前待夹物块、已抬升携带物和刚放下但底盘尚未离开的物块均临时排除；底盘离开后恢复为障碍。
- 运行验收：`/cube_obstacle_map` 1 个发布者、2 个代价地图订阅者；日志为 `Obstacle map published for 10 cubes, 0 fixed stones and 0 placement zones`。占用核心计数 `10 → 9（blue_cube_4 被选为目标）→ 10（失败状态恢复）`，目标排除/恢复正确。
- 当前不直接执行连续第二块 B 放置：替换后的简化操控服务虽能验证区域/静止，但尚缺多槽位与已放物块表面净距验收。下一步应只合并 Git 中的未占用槽位选择与净距校验，保留本轮已通过的两段式机械臂动作，避免把第二块放到首块附近或顶飞首块。

### 第二块蓝块连续回归的新证据

- 已给 B 区定向测试合并 Git 的占用槽位思想：只使用中心线候选 `(-0.18,0)/(0,0)/(+0.18,0)`，基于 Gazebo 真值拒绝与已放物块中心距小于 `0.085m` 的候选；A 区和机械臂轨迹未改。
- 首次连续调用发现测试状态发布器为 volatile、物块层订阅器为 transient-local，导致目标排除消息不兼容。已改为匹配 QoS；实测选中目标和启动暂缓物块同时生效，占用数为 8。
- Nav2 重启还会丢失上一轮 volatile 的放置完成事件。已增加仅启动时执行一次的近邻接管：底盘 `0.70m` 内的地面物块暂缓注入，底盘离开后自动恢复；不会在运行途中因小车靠近普通物块而放行。日志确认 `Startup footprint handoff excludes nearby cube(s): ['blue_cube_5']`。
- 第二块日志 `/home/ros/dev_ws/logs/git_pick_place_blue_to_b_validation_6_second.log`：起点致命栅格问题消失且路径可生成，但底盘从 B 停车姿态离场时，前方为刚放置物块、RPP `allow_reversing=false`，原地旋转持续触发 `collision ahead`；剩余距离约 `6.10→6.00→6.15m`，恢复 12 次后 180s 超时。任务在夹取前退出，未移动 `blue_cube_4`。
- 下一单一修复：恢复生产版“B 放置后离场”链路，先在保持朝向的情况下低速直线后退到安全净距，再交还 Nav2 规划下一取件路径；最后一件仍原地结算。禁止通过取消小物块保护或缩小实体核心来绕过该问题。

## 2026-09-07：地下物块/弹车根因、A/B 五槽回归与红 4 修复

- 蓝块落地下与弹车根因由 20 Hz Gazebo 真值复现：旧预放置姿态先把物块压到 `z≈0.025m`，同时将底盘抬到 `z≈0.0069m`、横滚约 `0.081rad`，不是松爪或退出阶段造成。预放置姿态已改为保持相同前向伸距、夹爪高约 `0.12m`；随后只执行一次垂直下放。
- 放置改为基于“物块相对 link6 的实际固定点”做刚体点 IK，并新增释放前/释放后 3D 硬门禁：物块高度、倾角、速度及底盘高度/倾角任一超限即保持夹持。蓝 2 精确验证日志：`/home/ros/dev_ws/logs/blue2_rigid_ik_20260907.log`。
- `PICKUP_PRE` 交接默认距离由 `0.25m` 提前到 `0.55m`。蓝 3 原先在距目标 `0.244m` 才取消 Nav2，制动期间越过物块；修复后即使物块进入侧后方，精对接也会先原地转向再调整距离，不再碾压。
- B 区按物块 ID 固定槽位，使用两排错位布局并按 3cm 实体加 2cm 净距验收，禁止按落点反推并复用槽位。最终五蓝累计日志：`detslot_blue_5_retry_20260907.log`、`idslot_blue_4_20260907.log`、`idslot_blue_3_20260907.log`、`idslot_blue_2_20260907.log`、`idslot_blue_1_20260907.log`。五件最终均在 B 区，`z=0.0147–0.0149m`、最大倾角 `0.0094rad`、速度为零；最后一件原地结算，前四件后续进场坐标保持不变。
- 新增目的区全库存和两两净距复检：每次放置后复查所有旧物块的区内位置、`z=0.012–0.020m`、倾角 `<=0.12rad`、速度 `<=0.03m/s`，并要求任意两件中心距 `>=0.05m`。该门禁已实际捕获旧 A 槽复用造成的红 2 下沉至 `z=0.0067m` 及红 1/红 4 间距仅 `0.048m`。
- A 区同样改为按红块 ID 固定的两排错位槽位。第一轮累计中红 5、红 2、红 3、红 1 均通过；红 4 在松爪前因预释放高度 `0.0401m` 被拦截，物块保持夹持。
- 红 4 的特殊夹持偏移使绝对 FK 高度模型失准。放置 IK 已改为在预放置位读取物块真实高度，只求到 `0.0215m` 的单次高度差，仍保持“预放置一次→垂直下降一次→校验一次→松爪一次”。同时将夹爪前向容差从 `0.028m` 调到 `0.036m`，侧向容差不变；这是因为红 4 实测前向 `0.033m`、侧向仅 `0.0047m`，旧门槛会误拒绝已夹住状态并在重试时推走物块。
- 红 4 最终定向验证 `/home/ros/dev_ws/logs/diffik_red4_forward036_20260907.log` 通过：预释放 `z=0.0296m`、底盘 `z=-0.0004m`，最终 `(A局部 0.288,-0.180)`、`z=0.0151m`、静止；普通夹持红 2 的新 IK 复测 `/home/ros/dev_ws/logs/diffik_standard_red_20260907.log` 同样通过，并复检既有红 4 不受扰动。
- 已证伪并完整撤销两项试验：将 Nav2 终点容差从 `0.15m` 收紧到 `0.06m` 未消除 map/world 定位偏置；基于一次 AMCL 位姿外推的坐标校准及区内真值转向精进位会因定位漂移/底盘滑移恶化误差。当前配置恢复 `xy_goal_tolerance=0.15m`，不保留额外区内转弯逻辑。
- 本轮未修改 Gazebo world、墙壁、石块、ABC 区坐标、小物块初始坐标或移动障碍物路径；运行中只记录，所有代码/参数修改均在任务结束后进行。当前下一步：在最终差分 IK 版本上再做一次完整十物块生产链回归，并统计任务耗时、Nav2 路径重规划次数与恢复次数；随后再做 CoStudio `1/5→5/5` 人工联调。

## 2026-09-07：最终差分 IK 五红累计与十物块生产链验收

### 完成的最小修复

- 抓取 docking 候选由含 45° 斜向姿态的 8 方向收敛为 4 个轴向姿态。失败证据表明红 4 的斜向抓取会让夹指先接触地面，使物块在预释放时仍高于允许范围；轴向正夹后不再出现这一现象。
- 携带物块导航固定使用仅在路径失效时重规划的行为树，禁用会在夹持状态执行原地 Spin/BackUp 的恢复动作。旧十物块第 3 件日志中，RPP 已连续报告前方碰撞，随后 Spin 恢复触发实体碰撞，底盘首次越界到约 `(-20.81,3.21)` 并继续弹飞；这不是放置失败。现在载物受阻时安全失败，不再用旋转恢复硬撞实体。
- 动态抓取只增加与抓取偏航有关、最大 `0.004m` 的高度补偿，处理同一物块因夹持方向不同产生的轻微固定点高度差；仍保持一次预放置、一次差分 IK 垂直下降、一次校验、一次松爪。
- B 区上排槽位仅向区域内部收进 `0.05m`（局部 y `0.18→0.13`）。旧 blue 4 实测预释放局部 y 为 `0.228m`，超出边界而被安全拒绝；新槽位保留更多边界余量，未放宽区域、接地、水平、静止或物块净距门禁。
- 所有修改均在任务非 active 时完成；Gazebo world、墙壁、石块、ABC 区、小物块初始坐标和移动障碍物轨迹均未修改。

### 独立五红累计复验

- 汇总日志：`/home/ros/dev_ws/logs/final_cardinal_red_summary_20260907.log`；分项日志：`final_cardinal_red_cum_1_20260907.log` 至 `final_cardinal_red_cum_5_20260907.log`。
- 结果：5/5 全部通过，总耗时 `322.258s`（5 分 22.3 秒），顺序为红 5→2→3→1→4；各项最大 Nav2 恢复数均为 0。
- 分项耗时：`54.826 / 82.133 / 57.715 / 79.936 / 47.623s`。五件最终高度 `0.0150–0.0151m`，全部水平、静止、位于 A 区且两两净距通过；最后一件放置后原地结算。

### 最终十物块生产链

- 汇总日志：`/home/ros/dev_ws/logs/final_ten_v5_summary_20260907.log`；分项日志：`final_ten_v5_item_1_20260907.log` 至 `final_ten_v5_item_10_20260907.log`；完整栈日志：`final_ten_v5_stack_20260907.log`。
- 结果：10/10 全部通过。总墙钟时间 `613.570s`（10 分 13.57 秒），分项执行时间合计 `613.529s`，所有分项退出码为 0、最大 Nav2 恢复数均为 0。

| 序号 | 物块 | 目标区 | 耗时/s | 最大恢复数 |
|---:|---|:---:|---:|---:|
| 1 | red_cube_5 | A | 71.178 | 0 |
| 2 | red_cube_2 | A | 90.878 | 0 |
| 3 | red_cube_3 | A | 60.820 | 0 |
| 4 | red_cube_1 | A | 68.658 | 0 |
| 5 | red_cube_4 | A | 55.594 | 0 |
| 6 | blue_cube_2 | B | 57.783 | 0 |
| 7 | blue_cube_4 | B | 50.703 | 0 |
| 8 | blue_cube_5 | B | 52.743 | 0 |
| 9 | blue_cube_3 | B | 55.108 | 0 |
| 10 | blue_cube_1 | B | 50.065 | 0 |

- 最终 A 区五件高度为 `0.0148–0.0150m`；B 区五件高度为 `0.0140–0.0151m`，最大倾角 `0.0042rad`、速度均为 `0m/s`。每次放置后的全库存复检均通过，未出现入地、弹飞、覆盖旧物块或实体碰撞。
- 第 1–9 件在安全验收后离场，第 10 件按要求不执行退出，放置后原地结算。
- 结论：最终差分 IK、最近物块调度、轴向抓取、载物避障、A/B 五槽放置、累计库存门禁和末件原地结算均已通过生产链回归。本轮不再调整导航或放置参数。

### 后续唯一未完成项

- 十物块验收后发现 u22 的 rosbridge 与 Windows `localhost:9090` 均未监听。任务已处于非 active 后启动 u22 rosbridge（PID `240305`），并建立带保活的隐藏 SSH 隧道（Windows PID `17668`）；TCP 复测为 `localhost:9090=OPEN`。服务日志：`/home/ros/dev_ws/logs/rosbridge_9090_20260907.log`。
- 当前仅剩 CoStudio 人工界面验收：确认 `/mission/execution_status` 驱动的 `1/5→5/5` 进度，以及相机、识别和三维画面同步。隧道已经可用，不需要再改 ROS 任务代码。

## 2026-09-08：动态轨道绕行、B 区停车及地图属性回退

- 按最新要求将聚合石块模型 `Untitled` 的 `<static>` 从 `1` 恢复为原始 `0`；模型及 13 个 link 的坐标、姿态和几何均未改。干净启动实测其父模型会在无碰撞时由 `y=-5.71233` 自行物理沉降至 `y=-5.940999`（约南移 `0.229m`）后静止，说明视觉偏移由动态模型的物理解算触发。后续碰撞一律从导航/避障修复，不再通过修改 stone static 或坐标处理。
- 两个移动障碍仍严格保持既定轨道与速度：上区 `(-2,2.8)↔(1,2.8)`、下区 `(5.5,-6)↔(8,-6)`、均 `0.25m/s`；未修改路径、速度或让行规则。
- 启动竞态根因之一是任务发布时 `/manipulation/execute` 仅有客户端、没有动作服务端。启动验收现要求机械臂/夹爪 active、AMCL/Planner/BT Navigator active、收到 `/amcl_pose`，并且 manipulation action server 数量为 1，任一条件不满足均禁止开跑。
- Fast DDS 多次重启后出现 SHM 端口锁及 ROS 图分裂（进程存活但节点/动作不可见）。清理脚本在确认比赛栈和 CLI daemon 全停后增加清理本用户遗留 `fastrtps_*` 文件；比赛栈与回归客户端改用 UDP-only Fast DDS profile。之后可见节点恢复至 45，`/manipulation/execute` 连续三次检测均为 1 个服务端。
- 夹取 Nav2→精对接交接距离由 `0.55m` 提前到 `0.70m`。旧 `blue_cube_3` 日志显示交接后物块一度进入车前 `0.243m` 危险近场并最终弹车；新日志在 `0.694m` 交接，精对接从 `x=0.847m` 平稳收敛到 `x=0.389m`。定向 `blue_cube_3→B` 通过：`59.497s`、恢复 0，最终 B 局部 `(0.076,-0.153)`、`z=0.0147m`、倾角 `0.0012rad`、静止。
- B 区第二列原局部 `x=0.28` 会把底盘停车目标推到约 `(-1.773,-5.149)`，底盘倾斜 `0.0496rad`、物块倾斜 `0.0609rad`，安全门正确拒放。第二列收回至 `x=0.18` 后，`blue_cube_1→B` 在停车目标 `(-2.001,-5.130)` 通过：`65.826s`、恢复 0；松爪前底盘倾斜 `0.0103rad`、物块倾斜 `0.0006rad`，最终 `z=0.0151m`、静止。
- B 北侧槽位局部 `y=0.16` 使 `blue_cube_2` 松爪前高度 `0.0365m`，超过严格上限 `0.035m` 1.5mm；未放宽安全阈值，只把该槽收回到 `y=0.10`。复测 `blue_cube_2→B` 通过：`75.962s`、恢复 0；停车目标 `(-2.009,-4.900)`，最终 B 局部 `(0.229,0.068)`、`z=0.0150m`、倾角 `0.0019rad`、静止。`blue_cube_5→B` 在已有蓝块条件下亦通过：`78.161s`、恢复 0。
- 参考《220s方案与参考1代码优点分析》加入 Nav2 发送前全局代价图探针。每条 pickup/transit/destination 均记录 `start_cost/goal_cost`；目标 cost≥99 时拒绝发送，避免主动进入静态致死单元。合法 `blue_cube_2` 实测 pickup/transit/destination cost 为 `0/0/0` 并完整通过；探针等待新鲜 Gazebo 位姿，不降低原 `0.35s` 新鲜度要求。
- 正式五件回归曾在 `red_cube_4` 直达路径与上区移动障碍轨道相交处失败：RPP 连续 `collision ahead`，随后 `Starting point in lethal space`，机器人被物理弹出地图。地图和移动障碍不改，只为该物块增加轨道西端外侧绕行：经 `(-2.75,2.00)→(-2.75,3.50)` 穿越，离轨道西端净距 `0.75m`，再沿轨道北侧进入抓取位。定向 `red_cube_4→A` 通过：`119.902s`、恢复 0、无再次碰撞或弹飞。

### 当前待完成

- 在上述版本上重新完成 `2红→A + 3蓝→B` 正式连续回归，记录每件耗时、恢复数、代价探针、最终库存几何和总时间；当前正确性单段证据已通过，但 `red_cube_4` 安全绕行耗时较高，尚不能证明整轮 `≤350s`。
- 正确性通过后，再借鉴 220s 方案做不改变地图的耗时优化：任务全局顺序/物块分配、风险区主备路线、与安全等待状态联动的 25s 停滞门控；一次只落地一个机制并回归。
- CoStudio 的 `1/5→5/5`、相机/识别/三维同步仍需最终人工验收。

## 2026-09-08 晚：220s 方案落地、最近优先与 350s 验收

### 已完成修复

- 依据 `220s方案与参考1代码优点分析.md` 增加全局代价地图目标探针、提前 handoff、红 4 上轨道西侧绕行，并保留严格放置验收。
- Nav2 抓取后曾因 VM 瞬时调度卡顿在等待 `follow_path` 应答时提前 ABORT，控制器形成孤儿目标；将 `bt_navigator.default_server_timeout` 从 500 ms 调整为 5000 ms。复测红 4 全链通过。
- 启动不稳定根因是旧 `cube_obstacle_map_node.py` 未被清理、多个实例同时发布；已将该进程加入精确清理白名单。清理后启动门禁一次通过。
- 连续放置第 4 件时 Gazebo 崩溃，日志为 `boost::shared_ptr<Link>::operator-> Assertion px != 0`，发生在 `detach_object`。根因是 IFRA LinkAttacher 在 detach 后未从 `GV_joints` 删除旧记录，后续物块命中悬空 JointPtr。现已在 detach 时校验 model/joint、按真实 joint 名删除并擦除记录；`ros2_linkattacher` 编译通过。
- 连续蓝 3→蓝 4 定向复测：58.652 s + 45.424 s，总计 104.108 s，恢复 0，Gazebo 不再崩溃。
- 完整五物块正确性基线：`final_5item_detachfixed_20260908`，红4→红2→蓝3→蓝4→蓝5，五件全部成功、恢复均为 0，总计 362.885 s；全部落地物块 z≈0.015 m、低倾斜、静止，最后一件原地结算。

### 新的最近优先规则与提速结果

- 验证驱动由 `fastest-颜色` 改为每件开始时重新执行 `nearest-颜色`，基于实时 Gazebo 机器人位姿选择尚未进入目标区的最近物块。
- 正式语义计划入口不再简单截取 YAML 前 N 个物块：计划生成时联合机器人、全部候选物块及 A/B/C 实时位姿做跨颜色最近贪心分配；每次放置完成后，执行器再以最新机器人位姿重排待执行任务。
- 保留 global/local costmap 的 `cube_layer`；队友 9.8 中删除小物块障碍层和全局实时障碍层的改动未采用，避免下一程碾压已经放入区域的物块。
- 中间件放置后直线退出仍保持 0.70 m、实时验证与已放物块间距递增，速度由 0.10 提至 0.20 m/s；最终件仍不退出。
- 队友 9.8 的 SIPP 决策、速度门、scan filter 与当前文件哈希一致；只借鉴空旷主航段提速方向。RPP `desired_linear_vel` 从 1.25 调为保守的 1.40 m/s（未采用 1.50 m/s），SIPP、曲率降速、Collision Monitor、末端低速和严格放置验收不变。
- 最近优先基线 `nearest_global_order_5item_20260908`：蓝5→蓝4→蓝3→红4→红2，5/5，恢复 0，总计 369.058 s。
- 提速验收 `nearest_5item_rpp140_20260908`：同一实时最近顺序，单件 48.208 / 50.845 / 58.433 / 74.928 / 95.562 s，全部 EXIT=0、恢复 0；总计 **328.063 s，TARGET_350=PASS**。相同顺序较基线缩短 40.995 s，距 350 s 留出 21.937 s。

### 关键证据日志

- `/home/ros/dev_ws/logs/final_5item_bt5000_cleanup_20260908_item_4.log`（旧插件在第 4 次 detach 崩溃）
- `/home/ros/dev_ws/logs/blue34_detach_record_cleanup_20260908_summary.log`（连续两次 detach 修复验收）
- `/home/ros/dev_ws/logs/final_5item_detachfixed_20260908_summary.log`（首次 5/5 正确性基线）
- `/home/ros/dev_ws/logs/nearest_global_order_5item_20260908_summary.log`（最近优先速度基线）
- `/home/ros/dev_ws/logs/nearest_5item_rpp140_20260908_summary.log`（328.063 s 最终验收）

### 保持不变的地图约束

- 未修改地图坐标、墙、石块几何/位置、ABC 区、物块初始坐标和移动障碍路线。
- 石块聚合模型 `Untitled` 继续保持 `<static>0</static>`；碰撞风险只由导航/避障处理。
- 移动障碍物 1、2 的端点和 0.25 m/s 速度未更改。

## 2026-09-08 最终干净重启与正式语义门禁审计

- 干净重启完成，精确进程检查：gzserver=1、gzclient=1、RViz=1、`cube_obstacle_map_node`=1，未发现 ROS 重名节点。
- AMCL、planner_server、controller_server、bt_navigator、collision_monitor 全部 `active [3]`。
- `joint_state_broadcaster`、`arm_controller`、`gripper_controller` 全部 active；`/manipulation/execute` 恰好 1 个服务端。
- `/amcl_pose`、`/global_costmap/costmap`、`/cube_obstacle_map`、`/scan_costmap` 均通过新鲜数据检查。
- 运行时实际导入 build/symlink-install 下的新源码；确认正式 coordinator 包含 `Greedy live assignment`，flow executor 包含放置后 `Re-evaluate`。
- `/mission/execution_status` 以 RELIABLE + TRANSIENT_LOCAL 成功返回 `IDLE / Waiting for /mission/run_flow`。首次 6 秒查询未收到是 DDS 发现窗口不足，20 秒有界复查约 3.5 秒即返回，并非进度话题故障。
- SIPP 链路无多发布者竞争：decision 节点只发布决策，velocity gate 唯一生成 `/cmd_vel_sipp`，Collision Monitor 唯一输出 `/cmd_vel`。
- 不驱动车辆的正式计划探针（x=2 红、y=3 蓝）输出：`blue_cube_5->B, blue_cube_4->B, blue_cube_3->B, red_cube_4->A, red_cube_2->A`，与 328.063 秒完整实测顺序一致；选择距离为 1.694 / 6.742 / 6.855 / 8.932 / 7.747 m。
- 启动日志唯一 ERROR 为 RViz Mesa/GLSL shader link 提示；RViz 保持单实例、进程正常，所有导航话题正常，因此不修改图形驱动或导航代码。

## 2026-09-09：A 区返回无效通道与红块选择修复

### 根因与修改

- 原“最近”算法只比较机器人到物块中心的直线距离，没有纳入 A 区门洞约束和四个可夹取停车位；因此在 A 内看似更近的 `red_cube_2` 会压过实际出门后更近的 `red_cube_3`。
- 现在当机器人处于 A 区时，以已验证的外门点 `(-2.30, 1.30)` 为共同起点，比较每个候选物块四个轴对齐夹取停车位的最短距离，并把每个候选分数写入日志。
- A 区中间件放置后不再直接把整条返程交给全局规划，而是严格通过 `(-4.00,-1.75) → (-2.25,-0.25) → (-2.30,1.30)` 的反向门洞序列，消除截图中的下方无效同伦路径。
- 另外补全 `red_cube_4` 夹起后的对称轨道绕行：`(-2.75,3.50) → (-2.75,2.00)`。日志证明原直达线穿过移动障碍 1 的 `y=2.8, x∈[-2,1]` 轨道，碰撞后机器人被弹到 `y=125.55`。本修复未改动障碍物路径、速度或地图。

### 定向验证证据

- 干净重启后确认：Gazebo=1、RViz=1、无重名 ROS 节点，Nav2、`arm_controller`、`gripper_controller` 与操作 action 门禁全部就绪。
- `red4_carried_rail_bypass_20260909.log`：红4绕过上半区移动障碍轨道后成功放入 A，水平、静止、倒车退出成功，用时 79.683 s。
- 放置红4后的选择探针：红3=`2.766 m`，红2=`4.336 m`，正确选择 `red_cube_3`。
- `red3_reverse_doorway_validation_20260909.log`：红3按三段反向门洞路线撤离 A，夹取、放置、倒车退出全部成功，用时 66.458 s，恢复次数 0。A 区红3/4均验收有效，中心距 0.367 m。
- 后续待做：以此版本进行一次完整五物块生产链，统计总时间和恢复次数，验证是否从 304.330 s 稳定进入 300 s 以内。

### 300 s 目标最终回归

- 首次完整链 `nearest_5item_a_return_rankfix_20260909`：蓝5→蓝4→蓝3→红4→红3，单件 47.835 / 48.725 / 54.875 / 92.538 / 60.727 s，5/5、恢复全为 0，总计 304.756 s。正确性通过，但比 300 s 多 4.756 s。
- 瓶颈日志显示，红4夹持后的两个轨道中转点耗时约 8.1 s 和 16.6 s，时间主要消耗在中转点的精确朝向收敛，而非实际位置通过。
- 只将这两个“夹持红4的轨道绕行点”设为 0.35 m 位置交接，路径坐标、安全边界、移动障碍运动均不变。红4定向复测 `red4_carried_handoff035_20260909.log`：70.761 s、恢复 0、放置与撤离通过，比上一版同场景快 8.922 s。
- 最终完整回归 `nearest_5item_a_return_rankfix_handoff035_20260909`：蓝5→蓝4→蓝3→红4→红3，单件 **47.223 / 54.055 / 54.294 / 76.585 / 59.862 s**；5/5、恢复全为 0，总计 **292.067 s，TARGET_300=PASS**，距 300 s 余量 7.933 s。
- 本轮验收确认：A 区返回严格经反向门洞，红3路线分数 2.766 m 优于红2的 4.336 m，红3被正确选中；红4携带时绕开上半区移动障碍轨道，未再发生碰撞弹飞。

## 2026-09-09：292 s 版本多轮重复性验收

### 三轮新增正式发布

- 第1轮 `repeat_290_round1_20260909`：单件 48.513 / 50.505 / 66.850 / 73.973 / 68.124 s，5/5，恢复全为0，总计 **308.026 s**。
- 第2轮 `repeat_290_round2_20260909`：蓝5=45.330 s、蓝4=53.247 s 成功；蓝3在 53.016 s 时于松爪前3D安全门禁中止，整轮2/5。失败根据为 `object_z=0.0383m > 0.035m`，机器人倾斜 0.0391 rad、物块倾斜 0.0497 rad，系统正确保持夹持、未危险释放。
- 第3轮 `repeat_290_round3_20260909`：单件 57.129 / 51.845 / 54.435 / 74.784 / 59.892 s，5/5，恢复全为0，总计 **298.149 s，TARGET_300=PASS**。同一蓝3槽位本轮松爪前高度 0.0306 m，倾斜符合门禁并成功放置。

### 统计与优化空间

- 计入之前的 292.067 s 验收轮，3个完整成功轮为 292.067 / 308.026 / 298.149 s；均值 **299.414 s**，中位数 **298.149 s**，极差 15.959 s。因此已可多次接近300 s，但尚不能证明稳定保持在290 s附近。
- 最大随机波动来自第3件蓝块的“B区撤离→返回夹取”粗导航：成功轮的主导航耗时约 7.82 / 19.18 / 7.82 s，跨度 11.36 s，且恢复数均为0，属于全局路径/动态时序切换而非恢复行为。
- 下一个候选单变量：为 B 区放置后返回夹取增加与送入路径对称的东侧通道位置交接，先做“蓝4放置→蓝3夹取”定向回归，只在稳定且确有缩时后并入正式版。
- 不建议通过放宽 `pre_release_object_z_max_m` 解决第2轮失败；0.0383 m 时松爪会增加跳动、碰撞或掉落风险。后续如再现，应检查B区底盘停车位姿与条件式稳定，而不是删除安全门禁。

## 2026-09-09：转弯连续性与夹取恢复优化

### 转弯卡顿证据与单变量

- RPP 原参数 `use_rotate_to_heading=true`、`rotate_to_heading_min_angle=0.60 rad`，约34°的中等转角也会触发“减速到零→原地转向→再起步”。
- 0.60 rad 红3→A 基线记录：总记录 58.247 s，原地转向 16.201 s，完全停止 19.199 s，角速度反转10次，平均运动速度 0.809 m/s。记录：`pick_place_red_cube_3_A_20260909_182448.csv`。
- 只将 `rotate_to_heading_min_angle` 提高到 `0.90 rad`（约52°）；依然保留90°急转的原地转向，速度、加速度、足迹、碰撞检测、地图和放置参数均不变。
- 0.90 rad 同段对照：总记录 53.650 s，原地转向 11.251 s，角速度反转8次，平均运动速度 0.824 m/s；缩短 4.597 s，夹取、放置及库存验收全部通过。记录：`pick_place_red_cube_3_A_20260909_182749.csv`。

### 夹取失败的有界重对准

- 首次0.90完整轮在最后红3遇到物理附着漂移：夹爪停车对齐为 `x=0.387,y=-0.001`，但 attach 后3个样本的工具相对位置从 0.107 m 漂到0.190 m。服务器回滚连接后只重试机械臂，未重新对准已移动的物块，第2次IK必然失败。
- 修复为：仅当夹取返回物理附着/夹取几何错误码5或7时，底盘对实时物块位姿重做一次 `fine_dock`，通过夹取窗口检查后再执行最后一次夹取。正常成功路径不触发，因此零新增耗时。

### 完整任务验收

- `turn090_reacquire_full_round1_20260909`：46.424 / 54.141 / 54.162 / 70.881 / 57.328 s，5/5、恢复全为0，总计 **283.017 s**。
- 中间一次重启因 localization 生命周期未激活被就绪门禁拦截，任务未发布，不计入完成率；清理残留 ROS CLI daemon 并重启后恢复。
- `turn090_reacquire_full_round2_20260909`：45.549 / 46.265 / 55.178 / 69.621 / 56.604 s，5/5、恢复全为0，总计 **273.295 s**。
- 当前正式版连续两轮完整成功，完成率2/2，平均 **278.156 s**，已达到约280 s目标，相比292.067 s版本分别缩短9.050 s和18.772 s。
- 当前不继续缩短机械臂的附着稳定采样、松爪前3D门禁和落地验收；已观测到附着漂移和放置高度超限，继续压缩会以完成率换取少量时间，不符合当前目标。

## 2026-09-09：红→B、蓝→A 逆向赛题回归

### 兼容性问题与最小修复

- 静态审计发现库存验收把 A 固定当作红区、B 固定当作蓝区。现改为复验目标区内全部红/蓝物块，因此支持逆向题目，同时正常红→A、蓝→B仍执行相同的0.60秒稳定门禁。
- 显式定向测试允许指定一个不属于默认颜色映射的物块；`nearest/fastest` 等正式选择器仍严格受 `task_mapping.yaml` 限制，默认映射文件哈希保持 `d52b6c2f...c40c299`。
- `蓝4→A` 首次在距离粗停车点0.694米时，Nav2尚未完成车头转向就交给精对准，物块落在车后方并被安全门禁拒绝。现要求精对准交接时物块必须已位于车头前方；正常进场本来满足，零额外固定等待。
- `红2→B` 首次携带后直达B东侧中转点，在 `(-4.34,1.62)`、剩余6.02米处无进展并以Nav2状态6终止。现仅对 `red_cube_*→B` 增加已验证的中央门洞位置交接 `(-2.30,1.30)→(-2.25,-0.25)`；正式蓝→B路线完全不进入该分支。

### 发布结果

- 第1组：`红4→B` 78.014秒、恢复0；`蓝4→A` 首次由车头方向门禁安全拦截，修复后55.055秒、恢复0；`红3→B` 52.123秒、恢复0。三项成功用时合计185.192秒。
- 正常路径对照：干净世界 `蓝5→B` 46.842秒、恢复0，与此前45.549/46.424秒样本处于同一仿真波动范围，未观察到逆向兼容代码造成正式路径变慢。
- 第2组：`红2→B` 首次暴露跨区路线缺口，修复后60.454秒、恢复0；`蓝3→A` 75.399秒、空载夹取段恢复1次但携带/放置恢复0；`蓝2→A` 69.437秒、恢复0。三项合计 **205.290秒**，全部完成。
- 最终代码覆盖6个不同逆向物块传送，均完成夹取、携带、停车、平放和库存验收。B区两红块中心距0.342米；A区蓝2/蓝3中心距0.302米，均水平、静止且位于区域内。

### 时间与后续判断

- 逆向任务需在A/B之间反复跨区，单件60～75秒主要来自真实通道距离，不应通过缩小安全距离或删除门洞约束换时间。
- `蓝3→A` 的一次恢复未在紧随其后的 `蓝2→A` 重现，因此不据此修改正式规划器。后续只有同一段连续复现时，才对该段做单变量优化。
- 地图、墙、石块、ABC区域、物块初始坐标、移动障碍路径与速度均未修改。

## 2026-09-09：逆向五物块原地转圈修复与整链回归

### 原地转圈根因

- 现场复现于 `red_cube_4` 北侧轨道绕行点和 B 区东侧中转点：Nav2 已到中转位置附近，`distance_remaining` 在 `0.00～0.21 m` 间往复，但恢复次数始终为 0。原因不是恢复行为或地图障碍，而是位置型中转点仍要求精确收敛一个对任务无意义的航向，MPPI 因而连续旋转/微移。
- 修复为：B 东侧中转点使用位置交接；`red_cube_4` 第二轨道点请求最小 `0.08 m` 位置交接下限，并保留基于实时速度的制动距离。最终夹取停车点、精对接和 B 区最终朝向仍严格验收，因此没有删除必要的对准动作。
- `red_cube_4` 夹取后增加西端原路退出 `(-2.75,3.50) → (-2.75,2.00)`，再进入中央门洞。此前直接切向门洞会穿越移动障碍物1的固定运动带，曾使底盘瞬间跳到 `(108.89,78.86)`；本修复没有更改地图或障碍物运动。

### 启动可靠性修复

- 干净启动曾出现夹爪插件加载超过 spawner 内部等待时间：控制器已经 loaded，却停留在 `unconfigured`。启动文件现于 spawner 退出后执行幂等状态检查，仅当控制器未激活时做 `unconfigured → inactive → active`；正常 active 状态不重复加载。
- 最终启动门禁确认：Gazebo GUI 1个、RViz 1个、无重名 ROS 节点；`joint_state_broadcaster`、`arm_controller`、`gripper_controller` 均 active；AMCL、planner、controller、BT navigator 均 active。

### 定向与完整回归结果

- 定向 `red_cube_4 → B`：约 78.0 s、恢复0；精对接 `x=0.388 m,y=-0.001 m`，放置后 `z=0.0152 m`、倾斜 `0.0199 rad`、速度0，倒车0.710 m成功。日志：`red4_spinfix_finalroute_20260909.log`。
- 完整逆向五物块：`blue5→A / blue4→A / blue3→A / red4→B / red3→B`，单件批处理用时 **51 / 72 / 72 / 79 / 59 s**，总计 **333 s**，5/5成功，所有段最大恢复次数均为0。
- A区三蓝块最终局部坐标分别约 `(0.141,0.016)`、`(0.298,-0.176)`、`(0.242,0.098)`；B区两红块约 `(0.121,0.122)`、`(0.100,-0.176)`。全部 `z≈0.015 m`、水平、静止，物块两两净距验收通过，五次放置后的倒车撤离均成功。
- 对应日志：`inverse5_spinfix_blue_cube_5_20260909.log`、`inverse5_spinfix_blue_cube_4_20260909.log`、`inverse5_spinfix_blue_cube_3_20260909.log`、`inverse5_spinfix_red_cube_4_20260909.log`、`inverse5_spinfix_red_cube_3_20260909.log`。
- 地图安装文件 SHA-256 仍为 `7e160715d89938f49287fb2806e7567fd4b60023b4c2a1c82d9fec2f2f7468e0`；本轮没有修改墙、石块、区域、物块初始坐标或移动障碍路径/速度。

### 关于“放置时提前规划下一段”

- 现有记录显示放置成功、0.70 m倒车撤离完成后，下一客户端启动间隔约1～2 s；主要耗时是实际撤离与跨区行驶，不是等待全局规划。
- 可提前计算下一物块排序和静态门洞序列，但不应在放置阶段缓存并盲走完整 Nav2 路径：移动障碍和局部代价地图在出发时已经变化。当前保留“先倒车调相，再在离开时使用最新代价地图规划”的安全结构；本轮通过取消中转点无效精确航向，直接消除了真正的长时间等待。

## 2026-09-10：270 s 目标达成与重复性验证

### 最终单变量优化

- 保留地图、墙壁、石块、ABC 区域、物块初始坐标和移动障碍物运动逻辑不变；计时使用 `race_mode:=true` 的单个 `gzserver`，不启动 Gazebo GUI、RViz 或 Foxglove，避免重复窗口占用资源。
- 导航末段采用 `0.15 m` 位置容差、最高 `0.13 m/s` 的低速正向进场，转向/前进阈值为 `0.28 rad`；最终朝向仍独立停车校验，角速度上限 `0.55 rad/s`、比例增益 `1.50`。
- Nav2 控制权交接的零速稳定时间从 `0.25 s` 收敛为 `0.15 s`；放置后倒车距离保持 `0.45 m`，速度由 `0.20 m/s` 提高到 `0.25 m/s`，每次均以实时里程和与已放物块的距离双重验收。
- 机械臂只缩短已经逐项定向通过的轨迹：`home→pregrasp=0.95 s`、`pregrasp→grasp/place=1.10 s`、`return_home=0.90 s`。两段式平放、夹爪物理约束、松爪前几何门禁及落地稳定验收全部保留。
- 修复连续物块操作的真实死锁：旧 `_sleep()` 在 action 回调内反复创建/销毁瞬态 ROS timer，第二件物块可能永久停在 `settling_for_attachment`。现改为在四线程 executor 中执行有界 `time.sleep()`，不涉及高频物块跟随；连续两蓝和完整五物块均跨过全部重复附着阶段。
- 启动文件删除循环式 controller 修复助手，避免 `/controller_manager/list_controllers` 服务风暴；控制器仍由官方顺序 spawner 启动。正式发布前继续检查三个 controller 和四个 Nav2 lifecycle 节点均为 `active`。

### 完整五物块计时

- 第 1 轮 `batch5_round5_270candidate_20260910.log`：蓝5→蓝4→蓝3→红4→红3，单件 **44.223 / 45.712 / 50.615 / 63.074 / 56.425 s**，总计 **260.054 s**，5/5、恢复全为 0，所有放置验收有效。
- 重复轮启动前发现旧启动组遗留 7 个孤儿 ROS 进程，造成 `perception/question/semantic/lifecycle_manager` 重名；首个导航 goal 在发送阶段被门禁超时，车辆未移动、物块未抓取，因此不计为赛题运行。精确清理遗留 PID 后，节点重名为 0，`/navigate_to_pose` 恢复为单一 action server。
- 第 2 轮 `batch5_round6_repeat260_20260910.log`：单件 **40.587 / 45.286 / 49.653 / 62.833 / 56.155 s**，总计 **254.522 s**，5/5、恢复全为 0；B 区三蓝和 A 区两红全部 `z≈0.015 m`、水平、静止、在区域内，最后一件原地结算。
- 两轮连续完整成功，完成率 **2/2**，均值 **257.288 s**，比 270 s 目标平均快 **12.712 s**；当前不再以放宽安全门禁换取几秒收益。

### 当前运行与待办

- 当前计时环境只有 1 个 `gzserver`，`gzclient=0`、`RViz=0`；不存在重复可视化窗口。
- 启动前置门禁必须同时检查：无重名节点、`joint_state_broadcaster/arm_controller/gripper_controller` active、`amcl/planner_server/controller_server/bt_navigator` active、`/navigate_to_pose` 只有 1 个 server。未满足时不得发布任务。
- 后续只需要在带单个 GUI/RViz 的正式展示模式做一次 CoStudio 人工验收；不要重复运行已经通过的 254～260 s 生产链，也不要再修改地图或缩小全局安全距离。

## 2026-09-10：CoStudio 赛题发布、本地大模型解析与端到端验收

### 根因与实现

- 原始 `可视化.json` 的 RawMessages、Indicator 和 Publish 面板均未填写 topic/path，发布按钮值为 `{}`，因此只能展示静态布局，不能形成赛题请求。
- 原语义节点只调用 DeepSeek，运行环境又没有 `DEEPSEEK_API_KEY`。现改为调用虚拟机已有的本地 `llama.cpp + Qwen2.5-Coder-0.5B Q4_K_M`，地址 `127.0.0.1:8081/v1`；启动文件在服务不存在时自动拉起，正常重启后不再依赖手工启动或云端密钥。
- CoStudio 自定义面板升级为 1.1.0：可编辑题目，设置红色变量 `x`、蓝色变量 `y` 的 A/B/C 映射和执行先后顺序，并通过 `/costudio/mission_request` 发布；任务 active 时禁止重复发布。
- 新增 `costudio_mission_gateway`：严格校验请求、变量名、非负整数和 1～5 件总数，保留映射顺序生成批任务，并复用已验证的 `navigation_pick_place_test --batch`。实时回传 `/mission/status`、`/mission/plan`、`/mission/current_task`、`/mission/execution_status`。
- 动态颜色映射只放开 `fastest-red/blue` 对任意 A/B/C 区的颜色候选；固定物块和原默认映射行为不变。地图、墙、石块、ABC 区域、物块初始坐标及移动障碍轨迹/速度均未修改。

### 干净启动故障与处理

- 第一次重启发现旧启动组未退出，旧 Gazebo、旧 coordinator/flow executor 与新 gateway 同时存在，造成 robot spawn 失败和 Nav2 生命周期配置失败。
- 按进程组精确清理两组比赛运行进程后，只启动唯一实例。最终确认：`gzserver=1`，旧 coordinator/flow executor=0，gateway=1；map server、controller server、BT navigator 均 active；arm、gripper、joint-state controllers 均 active；`/navigate_to_pose` 与 `/manipulation/execute` 可用。

### CoStudio 入口完整五物块验收

- 发布题目：红色数量为 `x=2`、蓝色数量为 `y=3`；映射顺序为蓝→B、红→A。Qwen 返回严格 JSON `variables={x:2,y:3}`。
- 自动选择顺序：`blue_cube_5 → blue_cube_4 → blue_cube_3 → red_cube_3 → red_cube_4`，每次均使用实时位置与路线代价重排。
- 结果：**5/5 成功、恢复 0 次、端到端 237.391 s**；执行器批处理 231.710 s。端到端时间从 CoStudio 请求进入开始，包含本地大模型解析与执行器启动开销，低于 300 s 目标和 5 分钟赛题时限。
- 五次均出现 `ACCEPTANCE PASSED`，无 `valid=False` 或 `FAILED`。最后两件 A 区红块落地约 `z=0.015 m`、倾斜 `<0.010 rad`、速度 0，两块中心距 0.213 m；B 区三蓝库存验收同样通过。
- 运行日志：`/home/ros/dev_ws/logs/costudio_mission_20260910_200533.log`。

### CoStudio 使用

- Windows 已安装扩展：`C:\Users\奶龙\.coStudio\extensions\nailong.competition-dashboard-1.1.0`；安装包位于 `C:\Users\奶龙\Desktop\算法\coStudio可视化\competition-dashboard\nailong.competition-dashboard-1.1.0.coe`。
- 布局文件：`C:\Users\奶龙\Desktop\算法\costudio\2026 比赛可视化.json`，包含比赛看板、识别图像、原始相机和三维路径面板。
- 使用 SSH 隧道时连接 `ws://localhost:9090`。CoStudio 已打开时按 `Ctrl+R` 重新加载扩展，即可看到“发布赛题与映射条件”。

## 2026-09-10：CoStudio 重置后重复计时验证

- 干净重置时发现上一次 launch 的孤儿 `gzserver` 未随父进程退出，会造成新实例 TF 树不完整、Nav2 无法激活。已按启动进程组和明确 PID 清理后重启为唯一实例；没有修改地图、障碍物或导航参数。
- 门禁确认后，经 `/costudio/mission_request` 发布相同赛题：本地 Qwen 解析 `x=2,y=3`，映射顺序蓝 3→B、红 2→A。
- 实际选择与执行顺序：`blue_cube_5 / blue_cube_4 / blue_cube_3 / red_cube_3 / red_cube_4`。五次均 `ACCEPTANCE PASSED`，恢复次数均为 0。
- 本轮 CoStudio 请求到结算 **240.665 s**，其中批执行 **234.817 s**，结果 **5/5**。日志：`/home/ros/dev_ws/logs/costudio_mission_20260910_205412.log`。
- 对比上一轮 237.391 s，波动 3.274 s，属于动态障碍相位与仿真调度差异；两轮均稳定低于 300 s 与 5 分钟赛题上限。
