# 2026-09-13 C 区分层回归与最小修复

## 固化范围

- C 区中心保持 `(7.0, -6.0)`，下半区移动障碍保持 `x=2.0, y=-3.0~-6.0, 0.25 m/s`。
- 未修改墙壁、石块、A/B 区、小物块初始坐标或全局安全半径。
- `navigation_pick_place_test.py`：按移动障碍实时 y 位置锁存 C 路线；两分支均在 `y=-1.0` 北侧安全横廊进入 `x=3.75` 东车道，再沿东车道南下。
- C 区放置使用四槽 2x2 网格：`x=±0.16`，南排 `y=-0.10`，北排 `y=+0.05`；保留两段式物理放置、槽位认领、实物占用检查、底盘代价过滤和放后直退。
- `wait_nav_ready.sh`：将一次性 map_server 生命周期探测改为最多 8 次有限重试，消除 Fast DDS 启动发现竞态。

## 证据

- `blue_cube_5 -> C`，旧东支路：77.326 s，0 次恢复，落点有效（远程 CSV `pick_place_blue_cube_5_C_20260913_182212.csv`）。
- 两件连续 C：184.349 s；`blue_cube_5` 与 `blue_cube_4` 均有效，中心间距 0.326 m，0 次恢复。
- 第四件旧北排目标 `y=+0.10` 在释放后到达 `y=+0.236`，超过机械臂服务真实边界 `0.225 m` 11 mm；因此仅将北排内收到 `+0.05`，未放宽验收。
- 旧 `y=-1.9` 斜切路径分别出现 `vx=6.7389 m/s` 和 `vx=14.3497 m/s` 弹飞；RPP 在碰撞前连续报告 `collision ahead`。静态栅格确认 `y=-1.0, x=0.35~3.75` 全为空闲。
- 最终北横廊东分支：`blue_cube_5 -> C` 95.774 s，0 次恢复，落点 `(-0.161,-0.015)`，有效。
- 同一最终东分支第二槽：`blue_cube_4 -> C` 87.207 s，0 次恢复；两件中心净距 0.258 m，均有效。
- 北排定向放置：`blue_cube_3 -> C` 99.464 s，0 次恢复，落点 `(-0.160,+0.090)`，有效。

## 尚未完成

- 最终 `y=-1.0` 路线下的四件连续 C 容量终验尚需再跑；本轮四件尝试在旧 `y=-1.9` 路径的第三件发生物理碰撞，不能计为最终版本失败。
- 后续只需运行四蓝连续 C，不再改槽位或地图；若通过，再进入混合 A/B/C 完整任务。

`test_logs/` 保存了成功与失败对照日志，可直接追溯上述结论。
