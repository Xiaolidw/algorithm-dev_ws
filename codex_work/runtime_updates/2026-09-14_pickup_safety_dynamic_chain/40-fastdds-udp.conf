[Service]
# Fast-DDS shared-memory lock files can survive an interrupted simulation and
# split the ROS graph during the next launch.  UDPv4 is deterministic on this
# single-host competition VM and avoids that stale-port failure mode.
Environment=FASTDDS_BUILTIN_TRANSPORTS=UDPv4
