setup(
    name='moon_warehouse_bringup',
    version='0.0.0',
    packages=find_packages(exclude=['test']),
    # 新增下面这一行
    scripts=['scripts/safe_goal_bridge.py'],
    data_files=[
        ...
    ],
    ...
)
